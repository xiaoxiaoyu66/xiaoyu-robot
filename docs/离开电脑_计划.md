# 离开电脑 · 落地计划

> 用户 2026-09-18：「后续的内容呢，到时候真正离开电脑。都要有计划」。
>
> 这条线的**方向**写在 [`总方案_v3_从现在开始.md`](总方案_v3_从现在开始.md) 第 5 节，
> 但那份写于 S5 / S5.5 / S6a / 情绪系统之前。**这份是更新过的落地版**：
> 每一步做什么、验收什么、花多少钱、几步做完、退路是什么。
>
> 开工前先读 [`../HANDOFF.md`](../HANDOFF.md)；只在做这条线时读本文件。

---

## 0. 全貌

    现在   笔记本一个人扛全部：麦克风 + 喇叭 + 摄像头 + 算力 + 屏幕
     A     跑满一周（0 元）              验的是"会不会自己掉线"
     B     脸搬去平板（0 元）            验的是"不接笔记本屏幕也能用"
     C     主机搬去 N100（要花钱）        验的是"不接键盘显示器也能活"
     C'    ESP32 当"身体"（几十~百元级）  脑子还在主机上，和 C 二选一，见 §4.3
     D     装进壳子（纯手工）
     E     随身（远期，不排期）

每一步的验收标准是同一句话：**把笔记本拿开，它还在。**

---

## 1. 最容易被漏掉的成本：N100 什么都没带

笔记本有麦克风、喇叭、摄像头、屏幕 —— **这四样都是白送的**，
而 N100 小主机一样都没有。搬过去要一样一样补上。

| 笔记本白送的 | N100 有吗 | 怎么办 | 预算（估算） |
|---|---|---|---|
| 麦克风 | ❌ 没有 | USB 麦克风（或带麦的 USB 音箱） | 50~200 |
| 喇叭 | 多半只有一个 3.5mm 口 | 3.5mm 小音箱，**音量固定** | 50~100 |
| 屏幕（脸） | ❌ 没有 | 平板当脸 —— 就是阶段 B 要做的事 | 0（pad 已有） |
| 摄像头（眼睛跟随） | ❌ 没有 | USB 摄像头；**不买也行**，关掉眼睛跟随，其它功能一点不受影响 | 50~150 |
| 算力 | ✅ N100 够（见 §5.1 算力账本） | — | — |

**主机 + 配件合计大约 800~1500 元**（主机 550~900 + 上面几样 + 支架外壳）。
这笔钱要花在阶段 C 之前，不是现在。

> 别一次买齐。**先买主机，到手先验证跑不跑得动（M2），不行就退，损失为零。**

---

## 2. A · 笔记本跑满一周（进行中，0 元）

**做什么**：什么都不做，让进程连续跑七天不重启。

**第一次开跑为什么失败（2026-09-18 查，已修）**：
原方案是「计划任务 → `scripts\run_forever.cmd`（守护循环）」。任务 XML 里埋着四个坑，
任何一个都足以把「跑一周」变成「跑一天」：

| 坑 | 默认值 | 后果 |
|---|---|---|
| `DisallowStartIfOnBatteries` | `true` | 拔一下电源线，任务当场被停 |
| `StopIfGoingOnBatteries` | `true` | 切到电池就杀 |
| `ExecutionTimeLimit` | `PT72H` | 第 4 天系统亲手把任务掐掉 |
| 启动的是 `.cmd` | —— | **必然弹一个黑窗口；顺手一关，守护和本体一起死** |

最后一个正是当天的真实死法：窗口被关掉之后，`logs\guardian.log` 里只有一条
`guardian: starting`，**再没有任何 `exited` 行** —— 进程不是崩的，是被连根拔的。

**已改成**：任务直接启动 `pythonw.exe scripts\guardian.py`。
`pythonw.exe` 起的进程天生没有控制台窗口，关无可关；另外三个开关在
`scripts\install_autostart.py` 里写死成关掉。原 `run_forever.cmd` 已删（活全被 `guardian.py` 接走）。

**起跑时间：2026-09-18 20:21:42**（重启后自启的那一刻 —— 计时从这里算）

三个坑都做了真机验证，不是"应该没问题"：

| 坑 | 怎么验的 | 结果 |
|---|---|---|
| 窗口能被关掉 | `MainWindowHandle`：守护和本体两个进程都是 `0` | 没有窗口，关无可关 |
| 拔电源被杀 | Kernel-Power 事件 105：20:16:37 拔电、20:19:48 插回；期间 20:18:41 查 pid | pid `28748` **没变**，uptime 连续 —— 切到电池不杀进程 |
| 重启不自己起来 | 20:21:26 开机 → 20:21:41 守护起来 → 20:21:42 本体起来 | 登录后 **15 秒**自动拉起，全程无人手动干预 |
| 断网就崩 | 用户手动断网实测 | 通过 —— 恢复后没退出（用户 2026-09-18 实测） |
| 半夜被 Windows 更新重启 | 用户确认这台机器**没有**自动更新 | 不适用 —— 下面「使用时段」那步手点提醒可以划掉 |

`logs\guardian.log` 到 2026-09-19 16:19 为止是两条 `starting`（手动起跑一次 +
重启自启一次）、**0 条 `exited`** —— 与预期完全一致。
（16:19 搬家断电后多了两条 `exited`，**都不是崩溃** —— 见 §2.2。）旧的 `.cmd` 时期混合编码日志已归档为
`guardian_cmd-era_20260918.log`，不再往新日志里掺 GBK。

**随时查活着没**：

```
py -3.11 scripts\install_autostart.py --status
```

**验收**：
- `--status` 里**「本体中途崩溃次数」应该是 0**；有一次 `exited` = 本体崩过一次，
  去查 `logs\error.log` 和 `logs\xiaoyu_*.log`。
  （注意：「守护启动次数」**不是**异常信号 —— 每次开机 / 重登录都会 +1。）
- `--status` 的「已连续运行」应该是 7 天上下，而不是几小时。
- `python -m xiaoyu --wake-report`：凌晨 0~6 点那一列接近 0（半夜没有自己醒）。
- 误唤醒次数在可接受范围（没有客观阈值，看体感）。

**退路**：一周内崩了不算失败 —— 崩了才有日志可查，这正是跑这一周的目的。

### 2.1 一定要跑满一周吗？

先把「这一周到底能买到什么」拆开看：

| 想验的东西 | 真需要 7 天吗 | 更划算的做法 |
|---|---|---|
| 内存 / 线程 / 连接泄漏 | **不需要** | 浸泡测试：把一周的对话量压进 1~2 小时，每小时记一次 RSS，半天就有斜率 |
| 偶发崩溃、状态机卡死 | **不需要**，但需要"轮数"不是"天数" | 同上。自然跑一周绝大部分时间是空闲的，轮数远不如压测 |
| 误唤醒（尤其凌晨） | **需要**，但 1~2 个通宵就够看量级 | 跑 2 晚，看 `--wake-report` 的 0~6 点那一列 |
| 系统的半夜行为（扫描 / 备份 / 休眠） | **需要** 1 个完整的凌晨 | 1 晚 |
| 你家环境的周波动（WiFi、电源、你不在家） | 需要 | 唯一真正只能靠时间的东西，风险也最低 |

结论：7 天里真正**不可压缩**的只有「凌晨的静默」和「环境的周波动」；
前者 1~2 晚就够，后者风险很低。剩下的都能用压测换时间。

所以两条路，按工期挑：

- **原版（7 天）** —— 成本其实接近 0：你什么都不用做，它在那儿跑着。
  唯一代价是「这周先不开下一件事」。不急就走这条，证据最完整。
- **压缩版（2~3 天）** —— 1 次浸泡测试 + 2 个通宵，然后对账收工。
  证据强度不比原版差（因为主动打了边界），缺的只是「周尺度漂移」。

一句话：**一周不是因为它值一周，是因为它免费。** 等得起就走原版；
想早点开工，压缩版够用，别为仪式感多等四天。

---

### 2.2 2026-09-19 搬家断电：计时被打断，但买到一条更值钱的证据

用户当天搬家拔了电源。**这不是白跑** —— 断电那一刻的日志把最担心的一条验掉了：

| 时间（09-19） | guardian.log 原文 | 怎么读 |
|---|---|---|
| 16:19:51 | `exited \| code=1073807364 \| 活了一轮 71892 秒` | 连续跑了 **19 小时 58 分**；`0x40010004` = 系统关机时终结进程，**不是崩溃** |
| 16:19:56 | `exited \| code=3221225794 \| 活了一轮 0 秒` | `0xC0000142`：守护 5 秒后去重启，可机器已经在关机流程里，python 连 DLL 都初始化不了 |
| 16:40:57 | 系统开机 | |
| 16:41:21 | `starting \| 守护 pid=10516` | 开机后 **24 秒自启**；16:41:36 本体起来并回到待机，全程无人干预 |

**顺带揪出两个真 bug（当天修完）**：

1. **仪表盘把「随系统关机退出」报成了「崩溃 2 次」**：`--status` 以前只数 `exited` 行。
   判定规则现在只写在 `xiaoyu/lifecycle.py` 一份 —— 关机类退出码算「随系统退出」，
   不计崩溃，并单独列一行。修复后实测：`本体中途崩溃次数：0（正常）`
   + `另有 2 次随系统关机退出（关机 / 注销时被系统终结，不算崩溃）`。
2. **守护会自己制造假崩溃**：重启前先问 `GetSystemMetrics(SM_SHUTTINGDOWN)`，
   确认系统真在关机就安静收工（关机是可以被取消的，所以先等 30 秒再下结论）。

**还查出一个会让验收报告失真的问题**：`--wake-report` 只扫 `*.log`，
而 loguru 每天零点把前一天压成 `.zip`（`xiaoyu/logger.py` 的 `compression="zip"`）
—— **一周跑完再看，能看见的只剩最后一天**，9-18 那次唤醒就是这么消失的。
现在 `.log` 和 `.log.zip` 都读；修复后立刻捞回 9-18 的 2 次唤醒（凌晨 0 次）。

**验收口径（2026-09-19 改）**：计时从**开机自启那一刻 2026-09-19 16:41:21** 重算，
**2026-09-23 16:41** 收工（4 天）。依据见 §2.1：7 天里不可压缩的只有「凌晨静默」，
而现在已经有 1 个完整通宵 + 「断电恢复」这条硬证据到手，没必要再赌一周。
判据不变：`--status` 的「本体中途崩溃次数」保持 0、凌晨 0~6 点接近 0、
守护启动次数只在开机 / 重登录后 +1。

**搬家后要重新确认的三件事**：笔记本插电（09-19 实测 `BatteryStatus=2`，在充电）、
合盖 = 不采取任何操作、平板与笔记本同网段（09-19 实测本机 WLAN 是 `10.35.76.134`，
平板那台 `192.168.1.147` **已经不在同一网段**，B 阶段开工前要重新配对）。

---

## 3. B · 脸搬去平板（下一步，0 元，1 次开工）

**为什么排在 N100 前面**：脸是唯一还需要一块屏的东西。先把消费端打磨好，
主机搬家时就不用再动它；而且它有当天的可见收益，不依赖任何采购。

**开工前必须知道的两件事（2026-09-18 查证）**：

1. ~~**脸页面现在没法在平板上打开。**~~ **2026-09-19 已解决**：
   主控现在同一个端口既接 WebSocket、也用 HTTP 伺服 `face/`（见下面「已做完」）。
2. **脸页面一变成 HTTP + 局域网 IP，平板的常亮就没了。**
   `navigator.wakeLock` 要求**安全上下文**（https 或 localhost），
   `http://192.168.x.x` 不算 —— 页面里那段 Wake Lock 代码在平板上会直接跳过，屏幕会自己熄。
   → 两条路：**(a) 平板系统设置里把息屏改成"永不"**（简单，推荐）；
   **(b) 用 Fully Kiosk Browser 这类自带 keep-screen-on 的浏览器**。

**已做完（2026-09-19，代码层，待重启生效）**：

| 做了什么 | 为什么 |
|---|---|
| 主控用 HTTP 伺服 `face/`，**和 WebSocket 同一个端口** | 平板打开 `http://<主机IP>:8765/` 就是脸；`location.hostname` 天然是主机地址 —— 配对零操作，也不用旁边再起 `python -m http.server 8080`（那个重启就没了） |
| 真 token 由服务端注入页面 | 平板不用手输 `?token=`；静态文件解析在新增的 `xiaoyu/face/static.py`，**目录穿越挡在这里**（`/../.env` 返回 404，不然局域网里谁都能把 API Key 打包带走） |
| 断线看得见：脸整块变灰 + 红点 + "断了，重连中……" | 以前是"脸还画着、其实早断了"，屏幕上完全看不出来 |
| 心跳 5 秒 + 页面 15 秒收不到就重连；重连 3 秒 → 1 秒；页面回到前台立刻重连 | 平板挂起会把 JS 定时器一起冻住，实测原来要 50 多秒才接回来 |
| 刚连上补推当前状态 | 以前要等"下一次状态变化"才知道现在什么状态，重连时主控多半在待机，屏幕上就停在"连接中" |
| 画布按屏幕缩放 + 乘 devicePixelRatio，字幕/状态改成浮层 | 原来写死 520x420 居中，平板上只占中间一小块、高分屏还糊 |

验证：真 Chrome 按 1280x800 和 800x1280 两种平板尺寸截图核对五个状态，页面零报错；
测试 268 → 290，两种跑法数字一致。**还没做**：重启生效（跑着的进程还是旧代码）
+ 真人验收，和 S6a 一起排在浸泡收工当天（2026-09-23）。

**做什么**：
1. ~~主控顺带伺服 `face/index.html`~~ —— **已做完**，现在浏览器打开
   `http://<主机IP>:8765/` 就是脸。
2. 平板上打开、加书签/主屏幕、全屏。
3. 平板系统音量**关到 0** —— 半双工铁律：脸的喇叭不许出声，出声会喂回麦克风。
4. 平板息屏改成"永不"（或换 kiosk 浏览器）。
5. 支架固定（脸要立着或斜靠，别平躺 —— 平躺人看不到）。

**验收**：把笔记本合盖放到一边，喊唤醒词 —— 平板上的脸有反应、说话对口型、
情绪变表情；**挂一整天不断线**（断线自动重连已有，这条是验它真的会重连）。

**退路**：平板太老、浏览器不支持 WebSocket/Canvas → 脸退回笔记本，其它一概不受影响。
这条线本来就是可选的。

---

## 4. C · 主机搬去 N100（要花钱，2~3 次开工）

### 4.1 M2 · 到手先验证，别急着迁（1 次开工）

**只做一件事**：装上 venv 和依赖，跑

    python scripts\bench_latency.py --no-llm

ASR / TTS 耗时可接受（预期 TTS <0.5 秒、ASR <0.5 秒）**再动手迁移**。
不行就退货，损失为零。**这一步的产出是把 §5.1 表格里的"推算"换成"实测"。**

### 4.2 M3 · 迁移（1~2 次开工）

按顺序：

1. **先定操作系统**（见 §6，这是搬之前最大的一个选择）。
2. 依赖装进 venv，`pip freeze > requirements.lock` **锁死版本**
   —— 现在的 `requirements.txt` 全是 `>=`，在 N100 上复现不出同样的环境。
3. 拷代码 + `data/xiaoyu.db`（它的记忆跟着走）。
4. **重新下载全部模型**（约 250MB，走 GitHub 1.28MB/s 那个老坑，预留一小时）。
5. 重新查音频设备名字，改 `.env`：
   现在是 Windows 的叫法（`XIAOYU_SPK_DEVICE_NAME=扬声器` / `XIAOYU_MIC_DEVICE_NAME=麦克风`），
   换系统后名字完全不同。**机制不用改** —— `xiaoyu/audio/devices.py` 的"按名字匹配"两边都成立。
6. 改成开机自启：Windows 沿用 `scripts\guardian.py`（`install_autostart.py` 注册的任务）；
   Linux 不用它，systemd 的 `Restart=always` 直接顶掉这层。
7. `python -m xiaoyu --check` 全绿 → 不接显示器键盘跑一天。

### 4.3 另一条路：先买「身体」而不是主机（2026-09-19 查的两个上游开源）

> 这一节和 C（买 N100）是**二选一**，不是前后关系：都是"换个硬件"，只是换的东西不同。

用户 2026-09-19 提了两个参考项目，查证后的结论：**它们不是竞品，是「身体」的现成件**，
正好补我们最缺的两块。

| 项目 | 事实（2026-09-19 查） |
|---|---|
| `78/xiaozhi-esp32` | 30,056 star / MIT / C++ / 138 个板卡目录、171 个固件变体 / 要 ESP-IDF ≥ 6.0.1 / ESP-SR 离线唤醒 / Opus 流 + WebSocket 与 MQTT+UDP / 硬件 AEC 可全双工 / 声纹 3D-Speaker / 设备端 + 云端 MCP / 38 语言 |
| `m5stack/StackChan` | 1,305 star；官方成品 **$99**；= M5Stack CoreS3（ESP32-S3 + 2.0" 触屏 + 摄像头 + 双麦 + 9 轴 IMU + NFC）+ 身体（**2 个舵机**：水平 360° / 垂直 90°，12 颗 RGB，550mAh 电池，红外）；固件 ESP-IDF v5.5.4，App 是 Flutter + Three.js 画脸 |

**我们缺、它们有的**：离线唤醒词（跑在板子上，不占 PC）、**硬件 AEC 全双工**
（我们只能半双工：说完才听）、Opus 端云音频协议、**实体动作**（舵机转头）、
设备控制协议（MCP）、声纹。**我们有、它们弱的**：记忆三层 + 情绪 + 性格这一套大脑逻辑、
误唤醒统计、进程守护与浸泡实测 —— 这些是小智后端「配置化」的插件，交给 mem0 /
RAGFlow 这类外部服务，不是自己长出来的。

**三条买法（花钱从少到多）**：

1. **平板（已有，¥0）** —— 先把第 3 节 B 阶段做完，脸搬过去。一分钱不花，体感提升最直接。
2. **ESP32-S3 开发板当「身体」（几十~百元级）** —— 烧小智固件（现成、MIT），
   我们只写**服务端**（Python，正好是我们擅长的）。「耳朵嘴巴」交给板子，
   「脑子」继续是我们的这套。**这是最匹配现状的一步**：不用碰 C++，
   却直接拿到离线唤醒 + 硬件 AEC。协议有官方文档（小智仓库的
   `docs/websocket_zh.md` / `docs/mqtt-udp_zh.md`），照着写后端就行。
3. **StackChan 成品（$99，淘宝也有）** —— 开箱就是个会转头的可爱机器人。
   代价：接我们的大脑要改 C++ 固件（ESP-IDF v5.5.4），是这三条里工作量最大的。

**为什么先别买 N100**：N100 换来的只是「同样的代码换个地方跑」；
而**身体买回来、笔记本当大脑就够用** —— 声音在桌上、脑子在隔壁房间，
这才是「离开电脑」的实际体感。N100 等身体跑通、知道要多少算力再买，钱花得更准。

**注意**：StackChan 仓库根目录没有 LICENSE（子目录各自 MIT），二次发布前看清楚；
小智是 MIT，参考和抄代码都放心。

**还没定**：走哪条。定了再动手，别同时开工 —— 出问题会分不清是新硬件的锅还是代码的锅。

#### 最终形态：照片里那种猫耳机器人（2026-09-19 用户给的目标）

用户发了张参考图：粉色猫耳壳 + 约 2 寸深色 LCD 脸（屏上画眼睛）+ 两条舵机手臂。
这是 Stack-chan / 小智 这一类"开源桌面机器人"的常见形态。

**先记住一件关键事实**：照片里那张脸是**设备固件自己画的**（C++ 往屏幕打像素），
不是浏览器。我们的脸是 HTML + Canvas，**在 ESP32 上跑不了** —— 所以"把身体套到硬件上"
之前，必须先拍一件事：**脸谁画**。

| 路线 | 脸谁画 | 硬件 | 代价 |
|---|---|---|---|
| **甲 · 保住我们的脸** | 还是我们的 Canvas | 能跑浏览器的设备（平板 / 旧手机 / Linux 小板 + 小屏） | 零重写；但形态做不小，塞不进猫壳 |
| **乙 · 跟开源生态** | 固件画（可换自定义资源） | ESP32-S3 + 小屏 + 舵机（照片同款） | 样子立刻对、最便宜；但表情/眼睛跟随协议要重做，或者写 C++ |
| **丙 · 折中** | 我们的设计，导成设备端资源 | 同乙 | 长相是我们的，实时动效受固件限制（口型/眨眼做不了那么细） |

**价（2026-09-19 从 M5Stack 官方店查的标价，美元）**：

| 件 | 价 | 备注 |
|---|---|---|
| M5Stack CoreS3 | $59.90 | 2.0" 触屏 + 摄像头 + 双麦 + 喇叭 + 9 轴 IMU —— 照片级别的"一体化头"；另有 Lite $44.90 / SE $38.90 |
| AtomS3R（0.85" 屏） | $17.50 | 太小做不了大脸，但能先验证端云协议 |
| Atomic Voice Base（麦 + 喇叭） | $5.95 | 配 AtomS3R 用 |
| SG90 舵机 | $5.50 / 个 | 照片里那两条手臂就是这个级别 |
| 8 通道舵机驱动 Unit | $6.95 | 也可以 GPIO 直驱，省掉 |
| StackChan 整机 | $99 | 开箱成型的对照品，见上面 §4.3 开头 |

**最容易漏掉的一项是壳**：自己没有 3D 打印机就淘宝代打印（小壳几十元），
或者买别人做好的套件。

**还没定**：走甲还是乙/丙。**等平板真人验收之后再拍** —— 到那时候才知道
"脸在真设备上挂一整天"还差什么。若最终就是要照片这个样子，乙/丙 最省事省钱（≈¥500 上下）。

**软件可以先做的适配（不花钱、不等硬件）**：

1. **抽出"身体接口层"**：屏幕 / 舵机 / 麦克风 / 喇叭 各一个抽象，行为代码不 import 具体
   驱动 —— 这正是 HANDOFF §8 里 `RON` 那个项目最值得学的地方，也是"把身体放到硬件上"
   的前置。
2. **给舵机写 backend**：先写一个**假舵机**（只记日志）+ 单测，真驱动买回来接上就行。
3. **把 `gaze(x, y)` 映射成转头角度**：我们已经算出来的眼睛跟随，立刻就能变成实体动作。

> **2026-09-19 进度**：上面 2 和 3 已经做完，1 里"舵机"这半边也做完 —— 代码在
> `xiaoyu/body/`，接法是 `app.attach_body`（`XIAOYU_BODY_ENABLED=1` 才开，默认关），
> 详见 [`HANDOFF.md`](../HANDOFF.md) 的「身体接口层 · 舵机这半边」。
> 屏幕 / 麦克风 / 喇叭的抽象仍然没抽：等真硬件定下来再抽，免得凭空猜接口。

### 4.4 眼睛跟想要单独确认

`XIAOYU_VISION_CAMERA=0` 指的是**第 0 号**摄像头。N100 上插 USB 摄像头后，
编号可能不是 0，要重新试。**不买摄像头就把眼睛跟随关掉**，其余功能不受影响。

**验收**：不接显示器、不接键盘，插电就自己起来；喊醒能聊；手机上能看到脸。
行为和笔记本一致 —— "一致"是硬标准，不许出现"N100 上偶尔不灵"。

**退路**：N100 实测跑不动 → 留在笔记本常驻，"实体化"降级成笔记本 + 支架，
**一个功能都不少**。这也是为什么 A 阶段必须先跑扎实。

---

## 5. D / E · 壳子和随身

**D · 外壳（纯手工，不碰代码，1 次开工）**：亚克力/3D 打印支架、走线、散热。
唯一值得加的"动"是点头舵机（SG90，40~70 元，纯装饰输出）。

**E · 随身（远期，不排期 —— 用户明确说以后再做）**：
- **前置条件：桌面版先稳定跑满一个月**，把功耗、发热、实际续航量出来。
  现在设计电池等于瞎猜。
- 要额外加：电池（PD 诱骗器 + 45W 以上充电宝，整机 10~20W，20000mAh 大概撑 3.5~7 小时）、
  外壳、散热、（可选）屏幕。
- **必须联网**：N100 跑不动像样的中文大模型，随身形态得挂手机热点，断网时它只能听不能说。
- 下单前如果已经想着随身：**优先挑原生支持 USB-C PD 供电的型号**，以后省一个诱骗器。
- 另一条路（**不建议**）：ESP32 + 服务器，等于换一个项目，而且把延迟敏感的环节搬到远端，
  和 2026-09-17 刚做完的提速是反方向。

---

## 6. 决策点：Linux 还是 Windows（搬之前先定）

| | Linux（Debian/Ubuntu） | Windows |
|---|---|---|
| 资源 | 省，开机自启干净，不需要显示器 | 4 核要被系统吃掉不少 |
| 音频 | 要重踩 ALSA / PulseAudio 一套新坑 | 已踩平，**代码一行不用改** |
| 代码 | 要改一行（见下） | 不用改 |
| 用户熟悉度 | 新 | 熟 |

**已知的代码改动点（2026-09-18 全项目扫过一遍）**：

- **`xiaoyu/vision/tracker.py:105` 用了 `cv2.CAP_DSHOW`** —— 这是 Windows 专用的摄像头后端，
  Linux 上必须改（按平台选后端）。**这是全项目唯一一处平台绑定的代码**，
  之前记的"代码零改动"要修正成"改一行"。
- `scripts/guardian.py` 本身跨平台（只用 `subprocess` + `pathlib`），Windows 专用的只有
  `scripts/install_autostart.py`（计划任务那部分）；Linux 上换 systemd unit。
- `xiaoyu/text.py` 读文件时会依次试 `utf-8-sig → gbk → utf-16`，在 Linux 上无害（第一项就中）。
- 其余全跨平台：`pathlib` 走路径、没有 `platform.system()` 改行为、没有平台专用依赖。

---

## 7. 预算与工期总表（估算）

| 阶段 | 花钱 | 开工次数（每次约 3 小时） | 前置 |
|---|---|---|---|
| A 跑满一周 | 0 | 0（等就行） | — |
| B 平板端 | 0（pad 已有，支架 20~60 可选） | 1 | A 跑起来 |
| C 主机搬 N100 | 800~1500（含 §1 全部配件） | 2~3 | 先买主机 → M2 验证 → 迁移 |
| C' ESP32 当身体 | 几十~百元级（板子 + 麦 + 功放 + 喇叭）；StackChan 成品 $99 | 1~2 | 先定买法，见 §4.3 |
| D 外壳 | 50~150 | 1 | C 通过 |
| E 随身 | 充电宝 + 外壳，另算 | 不排期 | 桌面版稳定跑满一个月 |

---

## 8. 退路总表（每一步都可以单独放弃，不影响前面的成果）

| 万一 | 退到哪 |
|---|---|
| N100 跑不动 | 笔记本常驻 + 支架，功能一个不少 |
| 平板太老跑不动脸 | 脸回笔记本，语音完全不受影响 |
| Linux 音频搞不定 | 换 Windows，代码一行不用改 |
| 摄像头买错/编号不对 | 关掉眼睛跟随，其余照常 |
| 整个 S6 不想做了 | 原判断仍然成立：**可以永远不买、永远不做** |