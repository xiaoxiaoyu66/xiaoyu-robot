# 交接文档（HANDOFF）

> **给下一个对话里的 AI 看的。开工前先从头读一遍。**
>
> 用户会开新对话框、让你读这个文件 —— 这是跨对话保持上下文的唯一手段。
> 所以这里**只写事实和实测证据，不写猜测**。改了代码就回来更新它，
> 尤其是"待办"和"现状"两节，过期的交接文档比没有更糟。
>
> 最后更新：2026-09-18 深夜（S5.5 常驻自愈 + 情绪系统 + 4c 向量检索完成；
> 全量 239 个测试全绿、两种跑法数字一致；修掉「测试污染生产日志」「情绪标记漏进正文」
> 「情绪回调接错签名」「历史教模型别写情绪标记 —— 表情整个不触发」四个坑；
> 表情脸修了两个几何 bug（豆眼高半只眼 / sad 视线累加飞出脸）并补上情绪嘴；
> 情绪也接进了控制台脸）
>
> **2026-09-19 追加（B 阶段平板第一步）**：主控现在同一个端口既接 WebSocket、
> 也用 HTTP 伺服 `face/` —— 平板打开 `http://<主机IP>:8765/` 就是脸，token 自动注入、
> 断线看得见、心跳 + 假死重连、按屏幕缩放；真 Chrome 按两种平板尺寸截图核对过，
> 测试 290 全绿。**待重启生效 + 真人验收**（和 S6a 一起，排在 09-23 浸泡收工当天），
> 详见 §6.4 B 阶段那条。
>
> **2026-09-19 追加**：用户搬家拔了电源 —— 一周计时被打断，但把「断电恢复」这条
> 验掉了（连续跑 19 小时 58 分 → 系统关机终结 `0x40010004` → 开机 24 秒自启，
> 原始日志见 §6.4）。顺带修掉两个「假崩溃 + 验收报告失真」的坑：`--status` 把
> 关机退出当崩溃报（判定规则现在只写在 `xiaoyu/lifecycle.py` 一份）、
> `--wake-report` 读不到 loguru 压过的 `.zip` 日志（一周跑完只剩最后一天能看）。
> 测试 251 → 268，两种跑法数字一致。验收口径改成 2026-09-23 16:41 截止（§6.4）。
> 另：**「离开电脑」这条线单独成文了** —— [`docs/离开电脑_计划.md`](docs/离开电脑_计划.md)
> 
> **2026-09-19 追加（身体接口层 + 硬件 A 档下单）**：新增 `xiaoyu/body/`（身体接口层：
> `ServoBackend` 接口 + 假舵机 + `gaze -> 转头角度`，接进 `attach_vision`，默认关；
> 真舵机买回来只换 backend），测试 290 -> **309**。同一天把 A 档硬件买齐了
> （¥95.15 全包邮：ESP32-S3 **N16R8** + INMP441 + MAX98357A + 2.4" ST7789 + 面包板 +
> 杜邦线 + 喇叭）。**硬件到货那天要干什么，直接看
> [`docs/A档到货开工.md`](docs/A档到货开工.md)** —— 那是给新对话看的冷启动指南
> （环境事实 / 到货验收 / 接线表 / 刷机 / 下一步写「小智设备协议」服务端）；
> 采购明细、逐件核对记录、踩掉的坑在同目录 [`docs/A档购物清单.md`](docs/A档购物清单.md)。
>
> **上游 `78/xiaozhi-esp32` 到底用它什么、不用它什么，见
> [`docs/xiaozhi拆解.md`](docs/xiaozhi拆解.md)** —— 决策边界都写在那（该做 4 件 + 不该做 8 条），
> 别重开讨论。
>
> **2026-09-19 追加（A 档开工 · 第 1/5 步）**：板子还没到，先按
> [`docs/xiaozhi拆解.md`](docs/xiaozhi拆解.md) §6 的动手顺序，从 §2.2.1「第一批交付物」
> 干起 —— 那一批**不用板子、不联网**。新增 `tests/fixtures/xiaozhi/`（22 个 JSON 真样本
> + README 溯源表），全部**逐字抄自**上游 `78/xiaozhi-esp32` 的 `docs/websocket_zh.md`，
> 生成时硬断言「这段文字必须在原文里」，抄错就当场失败。测试仍 **309**
> （unittest 与 pytest 数字一致）。下一步 `xiaoyu/xiaozhi/protocol.py`。

---

## 0. 一句话现状

**S0 / S1 / S2 / S3 全部通过了真人验证**（2026-09-17 夜）：
喊唤醒词能唤醒，说完话它能答上来，延迟体感可以接受。
**"反应慢"这件事也做完了**：TTS 换成本地 sherpa-onnx + DeepSeek 连接预热，
首句出声从 3~13 秒降到 **0.6 秒**。

**S4 的 4a（重启不失忆）和 4b（事实积累）都写完**（2026-09-18）：

- **4a**：启动时把最近的对话从 SQLite 读回上下文，并注入时间感。
  实测真实库 26 条 → 读回 20 条。
- **4b**：每聊够 `summarize_every` 轮（默认 20）在**后台**调一次模型，
  把"关于主人的事实"抽出来存进 `facts` 表。判重时把已有事实**连 id** 给模型。
  以前 `add_fact()` 一个调用方都没有，所以 facts 恒为 0 条。

**两条都已通过真人验证**（2026-09-18 晚）：
4a 关程序重开后答得出上次聊的内容、知道今天几号；
4b 调小 `XIAOYU_MEMORY_SUMMARIZE_EVERY` 聊几轮后，问"我叫什么"答得上来。
（代码层面 178 个测试全绿，真模型冒烟见下。）
不过 4b 已经用**真模型冒烟**验过一次：造 4 轮对话 →
抽出「主人叫小林 / 小林是大四学生 / 小林正在准备考研 / 小林不喜欢喝咖啡」4 条事实；
**再跑一遍全判成重复、一条不新增**。脚本 `.scratch/smoke_4b.py`（临时库 + 真 DeepSeek）。

**S5 表情脸 + 触屏打断、S6a 眼睛跟随也都写完**（2026-09-18 晚，详见 §6.3 之后）：
S5 已过真人验收（豆眼 Vector 风 + 终端字符画分身）；S6a 冒烟通过待真人验收
——摄像头读人头位置，待机/听话时它的眼睛盯着你看。
（原文这里写「全量 213 个测试全绿」，那个数**对不上**：当时 `unittest discover tests`
只收得到 204 个 —— `tests/test_gaze.py` / `test_emotion.py` / `test_memory_recall.py`
是 pytest 风格、压根没进闸门。已全部改写成 unittest 风格，现在两种跑法都是 251 个 ——
  后来又补了 12 个情绪接线 / 情绪位置的回归测试。）

**2026-09-18 还动了两个"外围"但影响体感的东西**：

- 唤醒词从 `小宇` 换成 **「小柚子」**（音素 `x iǎo y òu z i`）。
  顺带查明一件事：`小宇` / `小雨` / `晓宇` 的音素**完全一样**，
  模型根本分不出 —— 这就是以前误唤醒偏多的根因，三音节的词能治一大半。
- 音色短暂换过 **edge 云端** `zh-CN-XiaoxiaoNeural` + 语速 `+6%` + 音调 `+12Hz`，
  **当天晚又实测否决、默认改回本地 piper**（`.env` 的 `XIAOYU_TTS_ENGINE=local`）。
  edge 那套晓晓参数原样留在 `.env` 里，想听随时一行切回去。
  为什么否决：实测 edge 首句 1~3 秒**全是微软服务端生成时间** —— 预热无效、
  stream() 边下边播的首块 ≈ 整句下完的时间（微软整句生成完才吐数据）。
  完整数据见 §6.3。结论：edge 的慢买不到回来，只能二选一。

**S5 第一版已真人验收通过**（2026-09-18 晚）：WebSocket 协议
（state/mouth/caption + interrupt）、触屏打断、Vector 豆眼脸
（`face/index.html`：待机四处看、嘴跟音量、点脸立停）、
控制台字符画脸（终端里 `(-_-)` 状态行 + 口型 sparkline）全部验过。

**2026-09-18 深夜又收了一轮**：S5.5 常驻自愈、情绪系统、4c 向量检索三件都写完，
池子四项里只剩 **Live2D 换皮**和**完整 S6** 没动。细节见 §6.4。

**下一件不是写代码，是让它常驻跑一周。** 误唤醒几次、半夜会不会自己醒、
断网/拔插能不能恢复 —— S5.5 的代码全在，但**从没在真实环境里跑过一整天**。
零新功能，可这是唯一能把「演示」变成「设备」的动作。判据见 §6.4。

**顺序用户 2026-09-18 定过**：S6a → S5.5 → 池子 → **平板端** → N100（最后）。
用户同一天追加：**先把前两件做完，其他 TODO 往后排。**

**路线（用户 2026-09-18 定的）**：最终目标是**随身携带的独立个体**（不依赖电脑），
但**目前只做"桌面独立成体"**，随身以后再说：

    S4（4a ✅ → 4b ✅ → 4c 可选）  →  桌面独立成体（N100，插电放桌上）  →  （以后）随身形态

建议的顺序是：S4 收个尾（4c 可选），N100 那套硬件**同时下单**（要等快递）。
等货到了代码是稳定的，再专心搬环境 —— 两件事同时开工的话，
出问题会分不清是新功能的锅还是新环境的锅。详见 §6.2。


代码在 `D:\JavaAI\XiaoYu Robot`（**路径里有空格**，写脚本时记得加引号）。
远程仓库 <https://github.com/xiaoxiaoyu66/xiaoyu-robot>，分支 `main`，已同步。

---

## 1. 用户是谁，这个项目是什么

- 用户是 Java 出身的大四学生，**Python 较弱**，中文交流，回复用中文。
- 这是一个**纯爱好的桌面陪伴机器人**，不商用、不赶交付，每周大约 **3 小时**。
- 所以：宁可慢一点、讲清楚为什么，也不要用一堆他看不懂的抽象。
  **一次只改一件事，改完立刻验证。**
- 机器：Windows 11 / Ryzen 5 5600H / 16GB / RTX 3050 4GB / Python 3.11.9（系统级安装，**还没用虚拟环境**）。

---

## 2. 铁律（用户明确要求过，别违反）

1. **日志必须用好** —— 全项目禁止 `print()`，一律 `logger = get_logger(__name__)`。
   双通道：控制台彩色 + `logs/xiaoyu_YYYY-MM-DD.log`（DEBUG 全量）+ `logs/error.log`。
2. **API 密钥绝对不能硬编码** —— 读取顺序：环境变量 `DEEPSEEK_API_KEY` >
   `DEEPSEEK_API_KEY_FILE` 指向的文件 > `.env`。有 `tests/test_no_hardcoded_secrets.py` 自动扫描。
3. **代码放 `D:\JavaAI\XiaoYu Robot`**，注意保持现有目录结构。
4. **用 git 管好** —— 每次改完跑测试、提交、`git push`。
5. `.env` 已被 `.gitignore` 忽略，永远不要提交它。

---

## 3. 常用命令

```powershell
cd 'D:\JavaAI\XiaoYu Robot'

python -m unittest discover tests        # 跑测试（当前 251 个，全过）
python -m pytest tests                   # 同一批用例，数字必须一致（也是 251）
python -m xiaoyu --check                 # 环境自检
python -m xiaoyu --wake-report           # 按天统计唤醒次数（误唤醒率有数，S5.5）
python -m xiaoyu --text                  # 键盘模式，不碰麦克风/喇叭，验证 大模型+TTS
python -m xiaoyu                         # 语音模式：唤醒词 -> 录音 -> 识别 -> 回答
python scripts\check_audio.py            # 列出所有音频设备 + 录 3 秒回放
python scripts\diagnose_audio.py         # 挨个设备放提示音，找出哪个真的会响
python scripts\bench_latency.py          # 量延迟：TTS 合成速度 + 大模型首句（会真的调 API）
python scripts\bench_latency.py --no-llm # 只量本地 TTS，不联网、不花钱
```

**提交时的坑**：commit message 用 `git commit -F <临时文件>` 传。
直接写 `-m "..."` 时中文和引号容易被 PowerShell 吃掉。
写文件一律用 `[System.IO.File]::WriteAllText($p, $c, (New-Object System.Text.UTF8Encoding($false)))`，
否则会带 BOM。`apply_patch` 在这个带空格的路径下不可靠，改用 PowerShell + Python 精确替换。

---

## 4. 当前进度

### 已验证（有实测证据，可以信）

- S0 音频自检：用户亲耳听到回放，确认通过。
- S1 文字模式：`python -m xiaoyu --text`，中文输入 → DeepSeek 回复 → TTS 说出，
  扬声器正确落到 `[3] 扬声器 (Realtek(R) Audio)`。
- 单元测试 86 个全过；17 个模块导入正常。
- 三种提示音（ack / done / error）已在真实扬声器上播出。
- **延迟优化已实测**：`python -m xiaoyu --text` 日志显示"首句出声 0.61 秒"。
  本地 TTS 两个候选模型（piper-huayan / matcha-baker）都跑通，RTF 都在 0.06~0.08。
- **语音模式端到端：真人验证通过**（2026-09-17 夜）。用户原话：
  "唤醒词可以了，延迟也没那么明显了。"
  这一条从项目开始就一直悬着，现在可以放下了。
  顺带确认：唤醒词**音素级写法**（`x iǎo y ǔ`）在真机上是被听得到的。
- **接续对话：真人验证通过**（2026-09-18）。唤醒后连聊 3 轮、不说话回待机，
  用户确认已验过。这条从"只覆盖单轮"升级为"已验证"。
- **4a 重启不失忆：代码已跑通，实测证据如下**（2026-09-18，真机真库）。
  库里 26 条对话、上次聊天 `2026-09-17 22:04:10`；读回 20 条（= `max_history`）；
  实际组出的 messages 是：
  `[0] system 性格` → `[1] system "现在是 2026-09-18 周五 14:33。你们上一次聊天是昨天 22:04。"`
  → 20 条历史 → 本次提问。备份 `data/xiaoyu.db.20260918.bak` 已生成。
  **2026-09-18 晚真人验收通过**（关程序重开，问得出上次聊的内容、知道今天几号）。
- **4b 事实积累：真人验收通过**（2026-09-18 晚）：临时把 `XIAOYU_MEMORY_SUMMARIZE_EVERY`
  调小聊了几轮，问"我叫什么"答得上来。
- **测试 86 → 144 个全过**。新增 `tests/test_memory.py`（重启读回 / 备份 / facts / recall）
  和 `tests/test_llm_history.py`（历史读回 / 时间感 / 兜底话术）。
  补这个的原因：`MemoryStore` 在 `tests/` 里**原本一个引用都没有**（grep 零匹配），
  而 S4 三个台阶全都要动它。

- **S5 表情脸（协议 + 触屏打断 + 豆眼脸 + 控制台脸）：真人验收通过**（2026-09-18 晚）。
  用户原话：**"验收通过了"**（--text 模式：状态/字幕/口型跟随、点脸立停；
  终端字符画脸同步可见）。
  代码层面此前已冒烟：真起 FaceServer + 真 WebSocket 客户端验证错 token 被拒
  （关闭码 4401）、三类广播全收到、interrupt 事件 + 回调到位
  （脚本 `.scratch/smoke_face.py`，以后改了 face 就重跑它）。
- **测试 178 → 198 个全过**。新增 `tests/test_face.py`（协议/口型/打断事件）和
  `tests/test_speak_stream_interrupt.py`（触屏打断：中途停、关大模型流、标志保留给主控）。

- **S5.5 常驻自愈：代码完成，一周计时已开始**
  （2026-09-18 20:21:42 起跑；三个坑已真机实证：无窗口可关 / 拔电不被杀 / 重启 15 秒自启。
  **还没测**：断网一分钟。守护方案当天从 `.cmd` 换成了无窗口 `pythonw`，见下方 §S5.5 条目）。
  主循环自愈（拆出 `_one_conversation()`，单轮异常只记日志 → 等 2 秒 → 回待机，
  连续失败 5 次才退出并把原因写 `logs\error.log`）+ 自适应噪声底（开机采 0.3 秒，
  阈值 = 底噪 × 4.0，夹在 0.008~0.08）+ silero-vad 复判有没有人说话
  （模型缺失退回振幅启发式）+ 开机自启与进程守护
  （`scripts\install_autostart.py` / `scripts\guardian.py`）+ `WAKE_EVENT` 事件行
  与 `python -m xiaoyu --wake-report` 按天统计误唤醒。判据见 §6.4。
- **S5.5 断电恢复实测通过**（2026-09-19，用户搬家真断电）：连续跑 **19 小时 58 分**
  后被系统关机终结（`code=1073807364` = `0x40010004`，**不是崩溃**），开机后
  **24 秒自启**起本体并回到待机，全程无人干预。计时起点改为 2026-09-19 16:41:21，
  验收截止 **2026-09-23 16:41**（4 天，理由见 §6.4）。
- **情绪系统：代码完成 + 真模型冒烟通过**（2026-09-18）。
  冒烟脚本 `.scratch/smoke_emotion.py`：真 DeepSeek 跑一轮，回调拿到 happy/0.9、
  正文无标记泄漏、历史干净、语气联动进了下一轮 system。
- **4c 向量检索：代码完成 + 真编码验证**（2026-09-18）。
  真 fastembed 实测：问「他叫什么名字」→ 主人叫小林、「猫是什么颜色的」→ 橘猫、
  「他在忙什么」→ 正在准备考研，都对。**按判定停手，不再加深**（理由见 §6.1 P2）。
- **测试 198 → 234 个全过，且两种跑法数字一致**
  （`python -m unittest discover tests` 和 `python -m pytest tests` 都是 230）。
  原先 `tests/test_gaze.py` / `test_emotion.py` / `test_memory_recall.py` 是 pytest 风格的
  孤儿、根本没进闸门，已改写成 unittest 风格并补了流式回归用例。

- **A 档 · 小智协议真样本 fixture**（2026-09-19）：`tests/fixtures/xiaozhi/`
  22 个 JSON 全部逐字抄自上游 `docs/websocket_zh.md`（生成时断言 `body in 原文`，
  抄错即失败），带 README 溯源表（逐文件列文档章节 + 字节数 + sha256 前 12 位）。
  **纯离线**：不碰硬件、不联网。测试仍 **309**，unittest 与 pytest 数字一致。

### 未验证（别当成已完成）

- 提示音在真实唤醒流程里的体感效果（用户没提，说明至少不烦人，但也没说好）。
- `XIAOYU_SILENCE_SECONDS=0.5` 会不会把说话中间的停顿误判成"说完了"。
- **S6a 眼睛跟随**：冒烟通过，但没人拿真脸验收过（转头/低头跟不跟）。
- **S5.5 常驻自愈**：连续跑到 19 小时 58 分（2026-09-19 断电为止），**断电恢复已实测**；
  但还没跑满过验收窗口，「凌晨静默」目前只有 1 个通宵的数据。
- **自适应噪声底 + silero-vad**：从没在有底噪的真实房间里量过效果。
- **平板端（B 阶段）**：代码层做完并截图核对，但**从没在真平板上挂过** ——
  断线重连、常亮、触屏打断这些只有挂一整天才知道。
- S5 Live2D 换皮、S6 完整版：未开始。

### 里程碑

| 步骤 | 状态 |
|------|------|
| S0 音频自检 | ✅ 真人验证通过 |
| S1 能听会说 | ✅ 真人验证通过（文字 + 语音两种模式） |
| S2 有脑子 | ✅ DeepSeek 已接通，首句 0.6 秒 |
| S3 唤醒词 | ✅ 真人验证通过 |
| S4 性格 + 记忆 | ✅ 4a / 4b 真人验证通过；4c 向量检索已完成（2026-09-18），做完即停手 |
| S5 表情脸 | 🟢 第一版真人验证通过；情绪系统已完成第一层；**平板端第一步代码完成**（2026-09-19，待重启 + 真人验收）；Live2D 换皮未开始 |
| S5.5 常驻自愈 | 🟡 代码完成；断电恢复已实测（2026-09-19），**待跑满验收窗口**（截止 09-23 16:41） |
| S6a 眼睛跟随 | 🟡 代码完成、冒烟通过，**待真人验收** |
| S6 眼睛（完整版）/ 独立成体 | ⬜ 未开始 |

---

## 5. 已经踩过的坑（都修了，别重复踩）

这些都是**实测撞出来**的，不是理论风险：

1. **系统默认输出是显示器的 HDMI**。
   这台机器的默认输出是 `[3] H25T7-3 (NVIDIA High Definition)`，也就是显示器。
   声音全进了显示器，笔记本扬声器一声不响，而**日志看起来一切正常**。
   → 必须显式指定输出设备。

2. **音频设备编号会漂移**。
   同一台机器、同一次开机，两次运行之间 `[3]` 和 `[4]` **互换了**
   （扬声器 ↔ 显示器 HDMI）。PortAudio 的 MME 枚举顺序不是固定的。
   → 所以**按名字选设备**（`xiaoyu/audio/devices.py`），编号只当备用。
   这是最坑的一个 bug：写死编号会"时好时坏"。

3. **pygame 无法指定输出设备**。它的混音器自己挑设备，无视配置。
   → 已换成 `soundfile` 解码 + `sounddevice` 播放。

4. **孤立代理字符会把日志系统搞瘫**。
   从管道/文件拿到的坏字节会变成 `'\udc80'` 这类字符，
   它让 loguru 的**文件 sink 抛 `UnicodeEncodeError`**，整条日志丢掉、
   控制台刷一大段堆栈。→ `_redact()` 现在同时抹密钥 + 清编码，
   两个文件 sink 再加 `errors="backslashreplace"` 兜底。

5. **中文 Windows 上配置文件很容易被存成 GBK**（记事本"另存为 ANSI"）。
   写死 `utf-8` 读会直接抛 `UnicodeDecodeError`，报错新手完全看不懂。
   → `xiaoyu/text.py` 的 `read_text()` 自动识别 utf-8-sig / gbk / utf-16。

6. **中文里混进 ASCII 双引号 `"` 会截断 Python 字符串**，导致 `SyntaxError`。
   已经吃过一次（`check_audio.py`）。→ `tests/test_syntax.py` 防复发。

7. **唤醒模型文件是混搭的**：官方包里 int8 版本只有 encoder/joiner 没有 decoder，
   用 `sorted(glob())[0]` 会拼出组合不起来的三个文件。
   → 改成只挑"三件齐全且后缀一致"的一组。

8. **声码器和模型目录是并排放的，"旁边有 vocoder 就当 matcha"是错的**。
   sherpa-onnx 官方把模型都堆在 `models/tts/` 下：
   `models/tts/vocos-22khz-univ.onnx`（声码器）和 `models/tts/vits-piper-.../`（模型目录）**并排**。
   照"找得到声码器就按 matcha 加载"去判断，piper 会被当成 matcha，
   加载时报一个完全看不懂的错：`'use_eos_bos' does not exist in the metadata`。
   → 只能看模型**自己**的文件名：Matcha 的声学模型叫 `model-steps-N.onnx`，其余一律当 vits。
   `resolve_model_files()` 的 `_looks_like_matcha()` 就是这个判据，
   `tests/test_tts_engine.py::test_vocoder_next_door_is_not_matcha` 是它的回归测试。

9. **git 下载镜像只有 `https://gh-proxy.com/` 在这台机器上可用**（1.28 MB/s）。
   而且**必须带 User-Agent**，否则连接能建起来但永远不传数据（看起来像卡死）。
   `ghfast.top` / `ghproxy.net` / `gh.llkk.cc` / `github.moeyy.xyz` 全部失败。
   `scripts/download_models.py` 已经带上了 UA，也支持 `--prefix` 和 `--only`。

### 5.1 在这个环境里"干活"本身踩的坑

不是项目代码的问题，但一样费时间，而且**每次新对话都会重踩一遍**：

10. **PowerShell 不支持 heredoc**。写 `python - <<'PY'` 会直接报
    `Missing file specification after redirection operator`。
    → 先用 here-string 落一个临时文件再跑：

    ```powershell
    @'
    ...python 代码...
    '@ | Set-Content -Path "$env:TEMP\patch.py" -Encoding UTF8
    python "$env:TEMP\patch.py"
    ```

11. **PS 5.1 的 `Set-Content -Encoding UTF8` 会带 BOM**。Python 源码能忍 BOM，
    但 `.env` / `.md` / `.json` 不行。
    → 写这些文件统一用
    `[System.IO.File]::WriteAllText($p, $c, (New-Object System.Text.UTF8Encoding($false)))`。

12. **往"含中文"的补丁脚本里塞 ASCII 双引号，必炸** —— 就是第 6 条那个坑，
    2026-09-17 又踩了一次（改方案文档时）。
    报错是 `SyntaxError: invalid syntax. Perhaps you forgot a comma?`，
    而且 PowerShell 回显的还是乱码，很难看出是哪一行。
    → **补丁脚本里凡是含中文的长文本，一律用三层引号包住**（三引号那种），
    不要用单层双引号；真要引号就写中文的 `「」`。
    注意三引号里**不能再嵌同样的三引号** —— 外面用三个双引号，里面用三个单引号，反之亦然。
    2026-09-17 这一条连踩两次。

13. **管道 + `Out-String` 会缓存，看不到实时输出**。
    跑 `python -m xiaoyu 2>&1 | Out-String` 想看语音模式的进度，
    结果**屏幕一片空白**，很容易误以为程序卡死、然后去 Ctrl+C 一个其实跑得好好的进程。
    → **直接看 `logs\xiaoyu_YYYY-MM-DD.log`**，它是逐行落盘的。
    文件格式带毫秒时间戳，能直接算出每一步之间差了多少 ——
    上面那张延迟表就是这么量出来的。

14. **测试里等后台线程，不能只等"结果出现了"**。
    `warmup_async` 的回归测试一开始写成"等 `client.calls == 1` 就断言"，
    结果偶发失败：那时候线程还没跑到 `finally`，`_warmup_running` 还是 `True`。
    → 用 `_wait_idle()` 轮询到**整个函数真的退出了**再断言。
    这类"等一半"的测试是定时炸弹，宁可等久一点。

15. 源码里**不要再用 `\uXXXX` 转义写中文**。
    上一轮对话往 `xiaoyu/tts/engine.py` 里留了 164 处转义（运行时能跑，
    但源码里中英混杂，人读起来很累），已经统一还原成真中文。
    转义只在"绕 shell 引号问题"时才值得用，而这个仓库里**不需要** ——
    中文经 here-string 落盘是能正确保留的（第 10 条那个写法就是）。

16. **在这个环境里跑命令，终端输出可能整个看不到**（不是缓存问题，是拿不到 stdout）。
    跑测试时直接 `python -m unittest discover tests` 会"执行成功但没有任何输出"。
    → **重定向到文件，再用读文件的工具看**：

    ```powershell
    python -m unittest discover tests 2>&1 |
      Select-String -Pattern "^(OK|FAILED|Ran |ERROR:|FAIL:)" |
      Out-File -FilePath "$env:TEMP\ut.txt" -Encoding utf8
    ```

    然后用 Read 打开 `%TEMP%\ut.txt`。注意文件里的中文会是乱码
    （控制台按 GBK 解的），但 `Ran 144 tests` / `OK` / `FAILED` 这些行是好的，
    足够判断过没过；要看具体报错就往上翻 `ERROR:` / `FAIL:` 那几段。

17. **这个仓库的文件末尾没有换行符**。
    用精确替换的方式改文件时，`old_string` 如果带了尾随换行，会匹配失败并报
    "String to replace not found"（在 `llm/client.py`、`tests/test_text.py`、
    `memory/store.py` 上各踩过一次）。→ **收尾那一段不要带尾随换行**。

18. **中文关键词匹配要顾及空格**。写"超时要说一句人话"的异常映射时，
    关键词表里只写了 `timeout` / `timedout`，漏了带空格的 `timed out`，
    结果 `Request timed out` 认不出来（测试当场抓到）。
    → 这类映射表一律**把两种写法都列上**，别指望消息格式统一。

19. **"清掉脏数据"的循环要限定在收尾一条**。
    `_trim_dangling()` 一开始写成 `while` 循环丢掉结尾所有 user 消息，
    测试里"50 条 user 消息"那个用例直接把整段历史清空了 ——
    这种异常数据在真机上不该出现，但**静默清空记忆比留着一条脏数据糟糕得多**。
    → 只丢结尾那一条。

20. **跑这个项目的命令，必须用系统 Python 3.11.9，不能用别的 Python**。
    项目依赖（`numpy` / `sherpa-onnx` / `edge-tts` / `openai` / `loguru` …）
    全装在 `C:\Users\Administrator\AppData\Local\Programs\Python\Python311\python.exe` 里。
    **这个仓库还没有虚拟环境**（见 TODO"环境"一节）。
    用别的 Python（比如某些工具自带的 3.13）会直接 `ModuleNotFoundError: No module named 'numpy'`，
    而且报错点往往在 `xiaoyu/audio/player.py` 的 `import numpy as np`，
    **看起来像 Kokoro/TTS 崩了，其实是解释器选错了** —— 2026-09-18 在这上面白绕了一圈。
    → 调 `kokoro_speed.py` 那次 `exit=1`、日志全空，就是这么来的。
21. **非交互环境里 `git push` 会无声失败（exit 128、零输出）**（2026-09-18 实测）。
    认证本身是通的（服务端返回 200），崩在推送后 git 调 `git credential-manager store`
    存凭据那一步 —— GCM 在非交互环境下报错，把整个 push 拖死。
    → 绕法：`git -c credential.helper= push "https://<用户名>:<token>@github.com/..." main`，
    token 用 `"protocol=https`nhost=github.com`n" | git credential-manager get` 现取，
    **别写进任何文档**。用户自己开的正常终端窗口里没有这个坑。
22. **这个 AI 环境里 pip 直连 pypi.org 会读超时**，换清华镜像也连不上
    （沙箱只放行它自己的本地代理，git 走的就是那个）。
    → 装包用 `pip install --proxy http://127.0.0.1:<端口> 包名`，端口看 git
    curl trace 里的 `Trying 127.0.0.1:xxxxx`（本次实测 45895，可能会变）。
    用户自己的终端窗口里没有这个坑。
23. **pip 解析依赖回溯到 matplotlib 时会去下 35MB 的 sdist 源码包**（Windows 上
    编译必炸，装 mediapipe 时踩过）。→ 加 `--only-binary=:all:` 强制只用 wheel，
    解析器立刻老实。另外 mediapipe 自带 opencv-contrib-python，**别再装
    opencv-python**，两个 cv2 会互相覆盖。
24. **mediapipe 1.0 删了 `mp.solutions`**（2026-09-18 装到 1.0.1 实测）。只剩
    Tasks API：`BaseOptions` 在 `mediapipe.tasks.python.core.base_options`，
    运行模式枚举叫 `RunningMode`（不是 `VisionRunningMode`，vision 包 `__init__`
    里有导出名单），人脸检测器 `FaceDetector.create_from_options(...)`，
    BlazeFace 六个关键点 index 2 = 鼻尖。模型文件 *.tflite 不随包分发，要从
    `storage.googleapis.com/mediapipe-models/` 单独下（走本地代理）。
25. **这个 AI 环境里 Edit 工具出现过"报告成功但没写上"的假成功**（2026-09-18
    S6a 踩了三次：config.py 的 Paths、tracker.py 的 start()、导入块都是第二批
    编辑丢的，靠运行报错才暴露）。→ **一批编辑落地后，用 Grep/Read 抽查每个
    改动点再跑**；报错如果是"本该改掉的旧代码"，先怀疑这个，别怀疑逻辑。
26. **测试会往生产 `logs/` 写日志**（2026-09-18 实测）。`get_logger()` 一被调用
    就把文件 sink 建好了，所以**跑一次测试就往 `logs\error.log` 灌测试堆栈** ——
    实测里面 527 行来自测试，跑 3 个用例就涨 7KB。这会淹掉真实故障，
    也让 `--wake-report` 的误唤醒统计失真。
    → 修在 `logger.py`：发现 `pytest` / `unittest` 在 `sys.modules` 里，
    就把 `XIAOYU_LOG_DIR` 指到系统临时目录。**别写在 `tests/__init__.py`** ——
    `unittest discover tests` 是把测试模块当顶层模块导入的，那个文件根本不执行。
27. **流式里 token 边界会切在标记中间**（2026-09-18，情绪系统）。`[emotion]` 被切成
    `[` + `emotion]` 时，只认完整开标记的剥离函数会把 `[` 当正文发出去，
    整个标记跟着漏进正文/字幕/历史 —— **逐字喂必现，真 DeepSeek 冒烟也复现过**。
    → 尾巴判定要连"开标记的前缀残片"一起按住（`emotion.py::_holdback_index`），
    回归用例 `tests/test_emotion.py::StreamingHoldbackTest`。
28. **正常退出被记成"意外退出"，还带一串 asyncio 噪声**（2026-09-18）。
    `FaceServer.stop()` 原本用 `loop.stop()`，两个后果：
    ① asyncio 把它报成 `RuntimeError: Event loop stopped before Future completed`，
    而 `_run()` 的 `except Exception` 一律 `logger.exception` —— 于是**每次正常收摊
    都往 error.log 写一段假堆栈**；② socket 还没关就把循环停了，退出时满屏
    `Task was destroyed but it is pending!` / `Event loop is closed` /
    `coroutine 'Server._close' was never awaited`。
    常驻模式（守护反复重启）下这会迅速把 error.log 灌满噪声，把真故障淹掉。
    → 改成放行一个 future，让 `serve()` 自己 `server.close()` +
    `await server.wait_closed()`，收尾再取消残留任务、`shutdown_asyncgens()`、
    关循环（`face/server.py` 的 `_release_stop()` / `_shutdown_loop()`）。
    实测：起服务 → `stop()` → error.log 0 字节、stderr 无噪声。
    **顺手修掉一个假成功**：端口被占时 `start()` 以前照样报"服务已启动"，
    现在靠 `_bound` 事件如实报错 —— 常驻时不会"脸没了还以为有"。

---

## 6. 下一步 TODO（按优先级）

### ✅ 已完成（2026-09-17 夜）：TTS 换成本地 sherpa-onnx + 大模型连接预热

**为什么**：实测 edge-tts 出第一块音频要 **0.88 ~ 11.76 秒**，另外 2 次直接失败
（连接超时 / 握手失败）。**每句话都要重新握手一次**，回三句话就是抽三次奖。
这是"反应慢"的元凶，比大模型慢得多。

**怎么做**：用已经装好的 `sherpa-onnx` 做本地 TTS，不加新依赖。候选中文模型（官方列表里确认过）：

- `vits-melo-tts-zh-en` —— 中英混说，质量不错
- `matcha-icefall-zh-baker` —— 最快
- `kokoro-multi-lang-v1-1` —— 中英 103 音色，质量最好但模型最大

**预期**：首块音频从 1~12 秒降到 0.1~0.3 秒，而且不会失败。

**要求**：**保留 edge-tts 作为可切换的"高音质模式"**，别直接删掉 ——
本地音质不如晓晓。用配置开关切换（用户可以自己对比）。

**实际做法**：
- 新增 `xiaoyu/tts/engine.py`（引擎抽象：`SherpaEngine` / `EdgeEngine`，`build_engine()` 按配置选）。
- 重写 `xiaoyu/tts/synthesizer.py`：合成在主线程、播放在 daemon 线程，中间一个容量 2 的队列 ——
  **放第一句的时候第二句已经在合成**。
- 下载了 `vits-piper-zh_CN-huayan-medium`（60MB）和 `matcha-icefall-zh-baker`（72MB + 声码器 51MB），
  默认用 piper。切模型只改 `.env` 的 `XIAOYU_TTS_MODEL`。

**顺手做掉的第二件事：DeepSeek 连接预热**（这个才是"第一句话"最难受的那段）。
实测同一个问题，**第一次请求 2.0~2.3 秒，之后只要 0.5~0.8 秒**；
把历史对话从 0 轮堆到 40 轮，首句耗时几乎不变 —— 所以那多出来的 1.5 秒**全是建连接**
（TCP + TLS 握手），跟问什么没关系。加了 `DeepSeekClient.warmup()` / `warmup_async()`：
启动时异步预热一次，唤醒词命中后再异步预热一次（正好和录音+识别并行）。
实测首句 **2.05 秒 → 0.41 秒**。

**还没定的事**：用户要**盲听挑音色**，文件已经生成好了：
`data/voice_1_huayan.wav`（piper）和 `data/voice_2_baker.wav`（matcha），同一句话。
两个延迟一样，纯看喜好，听完改 `.env` 的 `XIAOYU_TTS_MODEL` 就行。

### ✅ 已完成（2026-09-17 夜）：真人验证语音模式

用户跑 `python -m xiaoyu` 喊"小宇"走完了一轮，原话：
**"唤醒词可以了，延迟也没那么明显了。"**

S1 / S2 / S3 到此全部通过真人验证。音色用户说**先这样，以后再优化**，别催。

顺带确认了一件事：唤醒词的**音素级写法**（`config/keywords.txt` 里是 `x iǎo y ǔ`
而不是汉字"小宇"）在真机上确实能被听得到 —— 第 5 节第 7 条那个坑的绕法是对的。

---

## 6.1 记忆（S4）—— 先做这个

**一句话**：现在的"记忆"是假的。程序一关，它就把你忘得干干净净。

### ✅ 已完成（2026-09-18）· P0 · 让它重启之后还记得你

**实际怎么做的**（改动落在 `llm/client.py` + `memory/store.py` + `text.py`）：

1. `DeepSeekClient._restore_history()`：启动时 `memory.recent_messages(max_history, include_time=True)`
   读回 `_history`，日志写明"恢复了 N 条历史消息（最早的是 X）"。
2. `_time_sense_message()`：往 system 里插一句
   "现在是 {日期 星期 时间}。你们上一次聊天是{昨天 22:04}。"
   `_last_seen_at` 在**启动时**取（`memory.last_message_at()`），时间每轮现取。
   相对时间的说法在 `text.py::describe_last_seen()`（零依赖纯函数，有单测）。
3. 读回时先过 `_trim_dangling()`：丢掉空内容、丢掉结尾那条没等到回复的 user 消息。
4. **顺手修掉一个老 bug**：`stream_reply` 的 `finally` 以前无条件把半截回复写进历史，
   现在只有 `completed=True`（流正常走完）才写。
5. **API 失败不再静默**：`_speakable_error()` 把 401/欠费/超时/限流翻译成一句人话
   （"我这会儿连不上脑子了，等一下再喊我"），认不出的异常照旧抛堆栈。
6. **每日备份**：`MemoryStore._backup_once_a_day()` → `data/xiaoyu.db.YYYYMMDD.bak`，
   **当天不覆盖**（当天第一份通常才是干净的那份）。

**验收（2026-09-18 晚已通过）**：关掉程序 → 重开 → 问"我们上次聊了什么"，
它答得上来，并且知道今天几号。

**顺带确认的事实**：`--text` 键盘模式**是写记忆的**（TODO 里原来写"不写"，那是错的）。
`run_text_mode` 把 memory 传给了 client，`stream_reply` 的 finally 无条件调
`remember_exchange()`。行为保留（一致、好排查），但调试时要注意它会污染上下文。

<details>
<summary>当时（2026-09-17）的诊断，留个记录</summary>

**为什么这是第一件事**：里程碑 S4 的验收标准是
"第二天它还记得你昨天说的事"。而这件事**离完成只差一行代码**：

- 对话**已经**全部存在 SQLite 的 `messages` 表里了（`memory/store.py` 一直在写）。
- 但 `DeepSeekClient.__init__` 里 `self._history = []` 是**空的开局**，
  而 `_build_messages()` 只用 `self._history`。
- `recall()` 又恒返回空（原因见 P1）。
  → **结论：关掉程序再开，它对你一无所知。** SQLite 里存的东西一行都没被读回来。

**2026-09-17 实测证据**（别凭感觉，跑一遍就知道）：

```
SQLite 里存着的对话条数: 26      <- 数据都在
SQLite 里的事实条数: 0           <- facts 表从来没被写过
recall() 返回: []                <- 所以检索永远空手而归
新进程里 _history 长度: 0        <- 而且历史根本没被读回来
实际发给大模型的消息条数: 2      <- 1 条 system(性格) + 你这句
```

也就是说：**它现在只有性格，没有记忆，而且连本次对话之前的自己都不记得。**

**怎么做**：

1. `DeepSeekClient` 启动时调 `memory.recent_messages(limit=N)` 灌进 `self._history`，
   日志里写明"恢复了 N 轮历史（最早的是 X 时间）"。
2. 加一点**时间感**：把最后一条消息的 `created_at` 一起带上，
   在 system 里补一句"你们上次聊天是 {昨天 21:30}"。它就会说出
   "上次你不是说在忙作业吗" 这种话 —— 这一句的体感价值远大于技术价值。
3. 别把几十年历史全灌进去，`max_history` 是 20 轮，取最近 10 轮左右就够。

**验收**：关掉程序 → 重新 `python -m xiaoyu --text` → 问"我们上次聊了什么"，
它得答得上来。答不上来就不算做完。

</details>

### ✅ 已完成（2026-09-18）· P1 · 自动记住"关于主人的事实"（4b）

原来的状态：`facts` 表建好了、`add_fact()` 写好了、`recall()` 也接好了，
但 **`add_fact()` 一个调用方都没有** —— 一条从头到尾健全、就是没接上电源的电路。

现在每聊够 `XIAOYU_MEMORY_SUMMARIZE_EVERY` 轮（默认 20）就在**后台线程**里调一次
DeepSeek，让它从最近对话里挑出长期事实，去重后写进 `facts` 表。

实现要点（**别被后人改掉**）：

- **判重时把已有事实连 id 一起给模型**：提示词里写成 `3. 主人在准备考研`，
  要求它直接输出"这条跟 id=3 重复"。比归一化字符串比对可靠得多，
  出问题也能顺着 id 回查到底在跟哪条比。
- **输出 JSON，但解析必须容错**：`_parse_facts()` 会剥 ``` 围栏、能忍前面的客套话；
  找不到 JSON 就当这轮没整理出东西。记忆是加分项，**绝不能因为它把对话带崩**。
- **丢后台线程**（`summarize_facts_async()`）：这一步要再调一次大模型（1~3 秒），
  同步做的话用户会看到"它话都说完了却卡着不动"。
- **限定条数**（一次最多 3 条、每条 30 字内），并在提示词里明确什么不值得记
  （天气、寒暄、当天的情绪起伏、推测出来的东西）。
- 调试：把 `XIAOYU_MEMORY_SUMMARIZE_EVERY` 设小（比如 3），聊几轮就能看到效果。
- 测试：`tests/test_fact_summary.py`（不联网，用假客户端）。全套测试 144 → 172 个。

**真人验证通过**（2026-09-18 晚）：调小 `XIAOYU_MEMORY_SUMMARIZE_EVERY` 聊几轮后，
问"我叫什么"答得上来。

### ✅ 已完成（2026-09-18）· P2 · 记忆第三步：向量检索（4c）—— 做完即停手

**怎么做**：`fastembed` + `BAAI/bge-small-zh-v1.5`（512 维、约 90MB、ONNX、
**无 torch** —— 原计划的 `sentence-transformers` 会拖进 200MB+ 的 torch，已弃用）。
事实向量缓存在 `fact_vectors` 表（`fact_id` 主键一对一，事实文本没变就不重算；
旧的空表 `vectors` 一并换掉）。`recall()` = 编码 query → 逐条 cosine → 取 top_k，
低于 0.25 当作不相关；**整条路失败就退回 4b 的"最近几条事实"**，
fastembed 没装 / 模型下不动都不影响记忆主流程（可选件静默降级）。
新配置 `XIAOYU_MEMORY_MODEL`。

**真编码实测**（`.scratch/diag_recall.py`，5 条事实）：

| 问 | 命中 | 相似度 |
|----|------|--------|
| 他叫什么名字 | 主人叫小林 | 0.502 |
| 猫是什么颜色的 | 小林养了一只橘猫 | 0.445 |
| 他在忙什么 | 小林正在准备考研 | 0.415 |
| 今天天气怎么样（无关） | 小林正在准备考研 | 0.267 ← 该被挡住却没挡住 |

**为什么做完就停手（别加深）**：

- facts 表现在就是这个量级（几十条）。这个规模上向量检索和"全量注入"没有
  可感知差别 —— 收益要等事实真的多起来才兑现，而"多起来"还没到。
- 真正的瓶颈是 **4b 的抽取质量和注入策略**，不是检索算法。
  阈值 0.25 偏松（无关问题也能蹭到 0.267 蒙混过关），但**拿 5 条事实调阈值就是过拟合**，
  先记着，等有了真实查询分布再调。
- 再加时间衰减 / 向量+关键词混合 / 评测集属于白投入。

### 🟡 P3 · 顺手观察（不用专门做，跑的时候留意）

- **接续对话**：唤醒后最多连聊 3 轮不说话回待机 —— 代码写了但没人验过。
- `XIAOYU_SILENCE_SECONDS=0.5` 会不会把你说话中间的停顿误判成"说完了"。
  会的话调大；反应慢就调小。
- `XIAOYU_TTS_SPEED=1.0` → 试 1.1，看是不是更精神（太快会显得敷衍）。

### ⚪ P4 · 以后的事

- **音色**：用户 2026-09-17 说"先这样，以后再优化"。
  `data\voice_1_huayan.wav` / `data\voice_2_baker.wav` 两个试听文件留着，别删也别催。
- **打断功能（barge-in）**：说话时喊唤醒词能打断它。
  **前提是先解决回声**：外放喇叭 + 开麦，它会听见自己。
  要么用耳机，要么上回声消除。参考 `Open-LLM-VTuber`。
- **S5 表情脸**：Live2D，旧手机/平板浏览器当屏幕，接状态机的 `on_change`。
  技术栈定为 Vue3 + pixi-live2d-display（2026-09-18 查证：库停更于 2023-12、
  **只支持 PixiJS v6**，pin 住 `pixi.js@6` 即可用，别追新；本机已有 Node 22 + npm）。
  用户 2026-09-18 问过"能不能用小新Pad 替代旧手机" —— **可以**，方案里本来就写的
  "旧手机/平板，能开浏览器的就行"。平板更大、音质更好，但**现在不用买**，
  先拿笔记本屏幕上的一个浏览器窗口把脸跑通，那时候才知道要多大、放哪个角度。
  两个到时候要注意的点：① 平板要开"充电时不锁定屏幕"，不然它会自己息屏、脸就黑了；
  ② 平板自己的喇叭要关掉/静音 —— 它会变成第二个声源，跟半双工那条铁律打架。
- **S6 眼睛**：RapidOCR 读字 + InsightFace 认人。搬 N100 的事见 §6.2。
- **虚拟环境**：用户之前选了"以后弄"。
  注意工作区里有个**空的 `.venv` 目录需要用户手动删**，`Remove-Item -Recurse` 被安全策略拦。

---

## 6.2 桌面独立成体（N100）—— 用户定的当前目标

> **落地计划单独成文：[`docs/离开电脑_计划.md`](docs/离开电脑_计划.md)（2026-09-18 晚更新）**。
> 那份是"谁做、做什么、验收什么、花多少钱、几步做完、怎么退"的完整版，
> 本节保留方向和判断。两处**本节没有的**要点先记在这：
> ① N100 小主机**没有麦克风、喇叭、摄像头、屏幕** —— 笔记本这四样是白送的，
>    搬过去要补买（合计约 800~1500 元）；
> ② 脸页面现在是本地文件打开的，**服务端不伺服 HTML**，平板打不开。

**用户 2026-09-18 原话**：

> 随身携带以后再做，目前先完成桌面独立成体。只是我未来想发展这个方向。

所以路线图是：**S4 记忆 → 桌面独立成体（N100，插电放桌上）→（以后）随身形态**。

### 为什么 N100 够用（2026-09-18 实测 + 推算）

在现在的电脑上用 **1 线程**跑，粗略模拟弱 CPU：

| | 现在的电脑（1 线程） | 现在的电脑（4 线程） | N100 估算 |
|---|---|---|---|
| 合成一句 2.9 秒的话 | 0.237s（RTF 0.081） | 0.120s | 约 0.25~0.45s |
| 识别 4 秒音频 | 0.255s | 0.109s | 约 0.3~0.5s |

N100 那列是按"单核约为 Ryzen 5 5600H 的 0.5~0.6 倍、4 核并行再打点折"**推算**出来的，
**不是实测**。换算成体感：一轮对话从 1.35 秒变成约 **1.8~2.2 秒**，仍在可接受范围。

**结论：整条链路（唤醒 + 识别 + 合成）N100 跑得动，不用加显卡。**

### 唯一绕不过去的硬约束：必须联网

N100 跑不动像样的中文大模型 —— 本地小模型中文质量差、速度也慢，装了等于白装。
所以：桌面形态要有 WiFi / 网线，随身形态必须挂手机热点。
**断网时它只能听、不能说。**

### 好消息：搬家的代码成本很低

2026-09-18 扫过一遍，**没有一个平台专用依赖**：

- 依赖全是跨平台的：`loguru` / `sounddevice` / `numpy` / `soundfile` /
  `sherpa-onnx` / `edge-tts` / `pypinyin` / `openai`。
  `pygame` 只出现在注释里（已经不用了）。
- 没有 `platform.system()` 之类的分支去改行为（`app.py` 只用它打一行日志）。
- 路径全走 `pathlib`，没有手拼反斜杠。
- 2026-09-18 又扫了一遍，**找到一个漏网的**：`xiaoyu/vision/tracker.py:105`
  用的是 `cv2.CAP_DSHOW` —— Windows 专用的摄像头后端，选 Linux 必须改成按平台选后端。
  所以上面的"代码零改动"要修正成"**只改这一行**"。
- **唯一要改的是 `.env` 里的音频设备名**：现在是 Windows 的叫法
  （`XIAOYU_SPK_DEVICE_NAME=扬声器` / `XIAOYU_MIC_DEVICE_NAME=麦克风`），
  换系统后名字完全不同。但**机制不用改** —— `xiaoyu/audio/devices.py` 的"按名字匹配"
  在两边都成立，只是要重新查一遍名字。

### 搬 N100 的待办清单（还没开始）

1. **买**。下单前必须确认的三件事：
   - **供电方式**：大部分 N100 小主机是 **DC 12V 圆口，不是 USB-C**。
     桌面形态无所谓，但如果以后要随身，就得配 PD 诱骗器 + 45W 以上充电宝
     （整机约 10~20W，20000mAh 大概撑 3.5~7 小时）。
     少数型号原生支持 USB-C PD —— **这种以后省事很多，值得为它多花点钱**。
   - 内存 ≥16GB、硬盘 ≥256GB。模型约 250MB；以后如果上 `sentence-transformers`
     还要再吃 2GB。
   - 无风扇还是带风扇 —— 塞进密闭壳体里，无风扇的会过热降频。
2. **定操作系统（还没定，这是搬之前最大的一个选择）**：
   - **Linux**（Debian / Ubuntu）：省资源、开机自启干净、不需要接显示器。
     代价是音频排查方式和 Windows 完全不同，**第 5 节第 1、2 条经验不能直接搬**
     （会换成 ALSA / PulseAudio 的另一套坑）。
   - **Windows**：用户熟悉，音频的坑已经踩平，代码一行不用改。
     代价是 N100 只有 4 核，Windows 11 自己就吃掉不少。
3. 重新装 Python + 依赖 + **重新下载全部模型**（约 250MB，还是那个
   GitHub 1.28MB/s 的坑，第 5 节第 9 条）。
4. 重新查音频设备名字，改 `.env`。
5. 做成**开机自启**（systemd service 或启动项），插电就跑，不用键盘显示器。
6. 在新机器上跑 `python -m xiaoyu --check` 和 `scripts\bench_latency.py`，
   把实测数字填回本节表格 —— **把上面的"推算"换成"实测"**。

### 随身形态（以后的以后，先把要点记下来别丢）

- **必须先做完桌面形态。** 随身版的功耗、发热、电池容量这些数据，
  只有桌面版跑起来才量得到；现在设计等于瞎猜。
- 桌面版跑通之后，随身版基本就是把同一套代码装进另一个壳子；反过来不行。
- 要额外加：电池（PD 充电宝 + 诱骗器）、屏幕（可选）、外壳、散热。
- 另一条路（**不建议走**）：ESP32 + 服务器，像 `xiaozhi-esp32` 那样把识别和合成放远端。
  用户正好有自己的服务器可以用，但它等于**换一个项目**，而且把延迟敏感的环节搬到远端，
  和 2026-09-17 刚做完的优化是反方向。

---

## 6.3 音色选型实测：Kokoro 跑不动、edge 救不回（2026-09-18）

**这一节回答一个问题**：默认 TTS 引擎用哪个。实测答案是**本地 piper**。

**第一条路：Kokoro 多语言模型**（`kokoro-int8-multi-lang-v1_1`，103 个音色，
含中文 `zf_` 女声 / `zm_` 男声）——奔着"本地 + 好听"去的，本机跑不动。

**引擎和下载都已经做好了**（改动都留用，没白做）：
- `scripts/download_models.py` 加了下载项，`--only kokoro` 可单独下。
- `xiaoyu/tts/engine.py` 的 `resolve_model_files()` 加了 kokoro 分支
  （判据是目录里有 `voices.bin`，**必须排在 matcha 判断之前**，否则会被误判 ——
  同第 5 节第 8 条那个"并排目录"坑的思路）。
  `ModelFiles` 顺便把单数 `lexicon` 改成了复数 `lexicons`（Kokoro 带两个词典）。
- `tests/test_tts_engine.py` 加了 6 个用例（`KokoroTest`）。

**但实测结论是：这台机器（Ryzen 5 5600H / 只有 CPU）跑不动它。**

| num_threads | sid=3 RTF | sid=10 RTF |
|---|---|---|
| 2 | 1.451 | 1.378 |
| 4 | 1.491 | 1.312 |
| 6 | 1.576 | 1.417 |

- **RTF ≈ 1.3~1.6**：合成 1 秒音频要花 1.3~1.6 秒，**比实时还慢**。
  对照 piper 的 RTF 0.06~0.08 —— **慢 15~20 倍**。
  edge 虽然要握手，但整句是一次性来的；Kokoro 是**每一句都真卡这么久**，体感更差。
- **加线程完全无效**（2/4/6 线程几乎一条线）：瓶颈不在并行度，救不了。
- 日志里一直刷 `Duplicated word ... Ignore it`（官方 lexicon 自带重复词条，无害），
  另有一个 `Unknown token`，不影响上面的结论。

**决策（2026-09-18 晚更新）**：Kokoro 和 edge 双双落选，**默认引擎改回本地 piper**
（`.env` 的 `XIAOYU_TTS_ENGINE=local`）。edge 保留为可切换的"高音质模式"，
晓晓参数（voice/rate/pitch）留在 `.env` 里没动。

**edge 为什么也救不回来（实测，别再试）**：当天为了"晓晓音色 + 低延迟"两全，
专门量过客户端还能不能榨出时间，结论是**不能**：

| 取法 | 冷连接 | 热连接（DNS/TLS 全热） |
|---|---|---|
| `save()` 整句落盘（现状） | 1.6~3.5s | 1.6~1.8s，**没快** |
| `stream()` 首块 mp3 | 1.5~2.6s | 1.8~3.6s，**更慢** |

- **预热无效**：第二轮连接全热，延迟不降 → 瓶颈不是握手（DeepSeek 当年预热有效，
  因为那是握手问题；edge 不是）。
- **边下边播无效**：stream() 首块只有 720 字节，到达时间 ≈ 整句下完的时间
  → 微软**整句生成完才开始吐数据**，服务端渲染时间，客户端怎么取都绕不开。
- 已装的 edge-tts 7.2.8 就是 PyPI 最新版，没有 raw PCM 输出参数，升级无路。

**N100 上更别指望**：N100 比 5600H 弱，本地 RTF 只会更差，edge 的 1~3 秒倒是一分不少。
想要"本地 + 好听 + 快"，在 x86 CPU 上目前没有便宜的两全解 ——
要么接受 edge 的服务端延迟（首句 ≈ 0.5 VAD + 0.14 识别 + 0.6 大模型 + 1.2~1.6 edge ≈ 2.4~2.8 秒），
要么用 piper / matcha（快、但没那么"甜"）。

---

### ✅ S5 · 表情脸 + 触屏打断（2026-09-18 真人验收通过）

1. **协议**（`xiaoyu/face/protocol.py`，纯逻辑）：state / mouth / caption 三类事件
   + interrupt 一条命令；token 校验（`?token=xxx`，错 token 关闭码 4401）。
2. **WebSocket 服务**（`xiaoyu/face/server.py`）：独立守护线程 + 自带 asyncio 循环，
   主控的串行循环零改动；广播走 `run_coroutine_threadsafe`，脸卡了不影响主控。
   websockets 延迟到 start() 才 import —— 没装这个包只是脸不启用，语音照常。
3. **播放器改造**（`xiaoyu/audio/player.py`）：`sd.play` → `OutputStream` 分块写，
   每 50ms 一块算 RMS（`mouth_level()`）推给脸当口型；`stop()` 在块间响应，
   触屏打断即刻停（旧版 sd.play 中途停不下来）。
4. **触屏打断**（`xiaoyu/tts/synthesizer.py` + `app.py`）：点脸 →
   `Synthesizer.interrupt()`（停声音 + 丢队列 + close 大模型流，半截不进历史）→
   主控等 0.2 秒余音散掉 → 开麦续听（半双工不破）。
   **键盘模式 `--text` 也接了脸**，调试脸不用开麦克风。
5. **最简脸**（`face/index.html`，单文件零构建）：**Vector 风 LCD 豆眼**
   （用户从 A 豆眼 / B 圆眼 / C 圆点 三版表情稿中选定 A 版，2026-09-18 晚重画）。
   深色脸屏 + bean 眼：待机时眼睛四处看 + 呼吸，listening 睁眼，thinking 上瞟，
   speaking 睁眼跳 + 嘴跟 mouth.level；随机眨眼；点脸发 interrupt；
   断线 3 秒自动重连；带 Wake Lock（平板不熄屏）。
   **这是"先完成"版**；Live2D 换皮（Vue3 + pixi-live2d-display，pin pixi.js@6）
   以后做，**以 A 版豆眼为设计基准**，协议不变只换消费端。
   情绪系统（用户 2026-09-18 提出：生气变眼神、开心眼角弯）设计骨架见 TODO，
   落地时给协议加 `emotion` 事件，脸这里加 mood 维度。
6. **控制台表情脸**（`xiaoyu/face/console.py`）：终端里的字符画分身 ——
   调试时人盯着 PowerShell，浏览器里的脸照顾不到，同一批状态/字幕/口型事件
   翻译成 `(-_-)` `(O_O)` `(=_=)` 状态行 + 逐句口型 sparkline（▁▂▃▄▅▆▇█），
   走 logger 同时进日志文件。默认开，`XIAOYU_FACE_CONSOLE=0` 关。
   和浏览器脸互不依赖（只订阅本机事件），可以只开一个。

**验收（2026-09-18 晚已通过）**：`--text` 模式开脸聊天 —— 状态变化、字幕、
嘴跟音量、点脸立停，终端字符画脸同步可见；用户确认通过。
配置在 `.env`：`XIAOYU_FACE_ENABLED / XIAOYU_FACE_PORT / XIAOYU_FACE_TOKEN`
（网页地址里的 `?token=` 要和 XIAOYU_FACE_TOKEN 一致）。

---

## 6.4 这一轮的三件事：S5.5 常驻自愈 / 情绪系统 / 4c（2026-09-18 深夜）

### S5.5 常驻自愈（代码完成，待跑一周）

对着 v3 §4 那几条做的：

1. **主循环自愈**：`run_voice_loop()` 拆出 `_one_conversation()`；单轮任何异常
   只记日志 → 等 2 秒 → 回待机，**连续失败 5 次才退出**，退出原因写 `logs\error.log`，
   交给进程守护重启。拆函数就是为了让"自愈"有一个清晰的边界。
2. **VAD 决断 + 自适应噪声底**：开机先采 0.3 秒环境噪声，
   静音阈值 = 噪声底 RMS × 4.0（夹在 0.008~0.08）；录完再用 silero-vad 复判
   （`sherpa_onnx.Vad`，模型 `models/silero_vad.onnx` 已在），
   推理失败就退回老的振幅启发式 —— 那是现状，不会更糟。
   **这步是为搬家做的**：N100 挂的 USB 麦噪声底和笔记本完全不同，固定 0.015 大概率失效。
3. **开机自启 + 进程守护**：`scripts\install_autostart.py` 注册计划任务
   （登录时跑 `pythonw.exe scripts\guardian.py`，**全程没有窗口**）
   + 电源设置（接电永不睡眠、合盖不休眠）；守护循环挂了 5 秒自己爬起来，
   日志写 `logs\guardian.log`。
   **2026-09-18 修过一次**：原方案走 `run_forever.cmd`，会弹黑窗口 ——
   窗口被关 = 守护和本体一起连根死（guardian.log 里只有 starting、没有 exited）。
   顺带把任务 XML 里另外两个「跑不满一周」的默认值也显式关掉：
   `DisallowStartIfOnBatteries` / `StopIfGoingOnBatteries`（默认 true，拔电就停）
   和 `ExecutionTimeLimit`（默认 PT72H，第 4 天系统掐掉）。
   还剩一步只能手点：Windows 更新 → 高级选项 → 使用时段（脚本会把步骤打出来）。
4. **误唤醒率有数**：唤醒时打一行 `WAKE_EVENT | ts=... | keyword=...`，
   `python -m xiaoyu --wake-report` 按天统计，并把**凌晨 0~6 点**单独拎出来
   （那时候你多半在睡觉，它醒了就是纯误唤醒）；>3 次/天就提示调阈值。

**顺带查到的事**：`logs\` 里翻到旧唤醒词（小宇）有
**6 次唤醒挤在 0.8 秒内**的记录 —— 换「小柚子」就是冲这个去的。

**验收怎么做（还没做）**：

1. 跑起来之后**拔插一次 USB 麦克风/音箱**，程序不该退出。
2. **断网一分钟**，恢复后要能接着用。
3. 然后**放着跑一周**，每天看一眼 `python -m xiaoyu --wake-report`：
   没人说话的日子应该接近 0 次；**半夜自己醒过几次是重点**。
   超过 3 次/天就调高 `config/keywords.txt` 里的阈值。

#### 2026-09-19 搬家断电：计时被打断，但买到一条硬证据

用户当天搬家拔了电源。这不是「白跑」—— 断电那一刻的日志把最担心的一条验掉了：

| 时间（09-19） | guardian.log 原文 | 怎么读 |
|---|---|---|
| 16:19:51 | `exited \| code=1073807364 \| 活了一轮 71892 秒` | **连续 19 小时 58 分**；`0x40010004` = 系统关机终结进程，**不是崩溃** |
| 16:19:56 | `exited \| code=3221225794 \| 活了一轮 0 秒` | `0xC0000142`：守护 5 秒后去重启，可机器已在关机流程里，python 连 DLL 都初始化不了 |
| 16:40:57 | 系统开机 | |
| 16:41:21 | `starting \| 守护 pid=10516` | 开机后 **24 秒自启**，16:41:36 本体回到待机 |

**顺带揪出两个真 bug（当天修完，测试 251 → 268）**：

1. **仪表盘把「随系统关机退出」报成了「崩溃 2 次」**。判定规则现在只有一份，
   写在新增的 `xiaoyu/lifecycle.py`：关机类退出码（`0x40010004` / `0xC000013A`）
   算「随系统退出」，不计崩溃，单独列一行。修复后实测输出：
   `本体中途崩溃次数：0（正常）` + `另有 2 次随系统关机退出（不算崩溃）`。
2. **守护在关机时会制造假崩溃**：重启前先问 `GetSystemMetrics(SM_SHUTTINGDOWN)`，
   确认系统真在关机就安静收工，不重启（关机可以被取消，所以先等 30 秒再下结论）。

**还查出一个会让验收报告失真的问题**：`--wake-report` 只扫 `*.log`，
而 loguru 每天零点把前一天压成 `.zip`（`xiaoyu/logger.py` 的 `compression="zip"`）
—— 一周跑完再看，能看见的只剩最后一天（9-18 那次唤醒就是这么消失的）。
现在 `.log` / `.log.zip` 都读；修复后立刻捞回 9-18 的 2 次唤醒（凌晨 0 次）。

**验收口径改为**：计时从开机自启那一刻 **2026-09-19 16:41:21** 重算，
**2026-09-23 16:41** 收工（4 天）。判据：`--status` 的「本体中途崩溃次数」保持 0、
`--wake-report` 凌晨 0~6 点那一列接近 0、守护启动次数只在开机 / 重登录后 +1。

**搬家后要重新确认的环境**：笔记本插电（09-19 实测 `BatteryStatus=2` 在充电）、
合盖 = 不采取任何操作、平板与笔记本同网段（09-19 实测本机 WLAN 是 `10.35.76.134`，
平板那台 `192.168.1.147` **已不在同一网段**，B 阶段开工前要重新配对）。

### 情绪系统（第一层完成，就做到这里）

模型在每轮回复**末尾**输出 `[emotion]happy,0.8[/emotion]`，跟着流式正文一起回来 ——
零额外调用、零额外延迟，而且正文和情绪同一次生成，语义必然一致。

- `xiaoyu/llm/emotion.py`：`split_visible()` 流式剥离标记、`parse_emotion()` 解析，
  认不出来一律 neutral。**纯函数、可单测。**
- 协议加 `{"type":"emotion","mood":..,"intensity":..}` 增量事件；
  老的脸对未知 type 直接忽略，协议天然向前兼容。
- 脸按 mood 改眼形 + 光环色：生气锐眼红光环 / 难过垂眼蓝灰 / 开心弯眼暖金 /
  惊讶瞪圆青；12 秒没新事件回 neutral。
- 上一轮情绪回灌下一轮 system 做语气联动，但明确要求**只表演、不伤人**。

**用户实测后又修的一个接线 bug（2026-09-18 深夜）**：情绪回调当时是这么接的 ——

    on_emotion = _caption_emitter(face.publish_emotion)

那个 `_caption_emitter` 是给**字幕**用的转发器，按字幕签名写死了 `emit(text)`；
情绪是两个参数，真跑必炸

    TypeError: emit() takes 1 positional argument but 2 were given

最阴的地方：异常被 `stream_reply` 兜住了（情绪是表演、绝不能带崩对话），
只在 `logs\error.log` 里留一行 —— **聊天一切正常，就是脸永远不变表情**，
用户不问根本发现不了。

为什么之前的冒烟没抓到：`.scratch/smoke_emotion.py` 当时自己写了个 `lambda m, i: ...`，
签名是对的。**自接一遍的冒烟，测不出"线上接错了"。**

→ 修法：转发器改名 `_fanout(label, *handlers)`、参数开放（`emit(*args)`）；
`app.build_callbacks(face, console_face)` 把「字幕 + 情绪」的接法收成一份，
键盘模式和语音模式共用；冒烟脚本也改成调它。
回归测试 `tests/test_emotion_wiring.py` —— **从 `build_callbacks` 出发、
走真的 `stream_reply`**（塞个假 openai 流进去），断言情绪真的落到了脸上。
这个测试验证过它抓得住：把 `emit(*args)` 改回 `emit(text)`，4 个用例立刻红。

**用户实测："表情不触发" —— 真因是历史把格式带歪了（2026-09-18 深夜，已修）**

用户的疑问是「表情该怎么触发？我聊天日志里什么都没有」。分三层查出来：

1. **日志确实是瞎的（已补）**。情绪只在浏览器脸上变，终端和日志里一个字都没有，
   所以"到底有没有触发"根本无从判断 —— 这不是用户看漏了，是压根没写。
   现在 client 会打 `情绪：happy（强度 0.70）-> 已发给表情脸`，
   或者 `这一轮模型没给情绪标记，按 neutral 处理（脸不变表情）`；
   控制台脸也加了 `心情 | (^_^) 开心 · 强度 0.7`。
   （控制台脸渲染情绪原本记在"以后再做"，这次提前做了 —— 不看见就没法调。）
2. **链路本身是通的**。起真脸服务 + 真浏览器（msedge），发一条情绪，
   页面 4/4 收到并发回 mood（`.scratch/e2e_emotion_face.py`）。
   所以不是 WebSocket、不是协议、不是脸的问题。
3. **真正的病根：对话历史在教模型别写标记。**
   历史里存的是**剥掉标记之后**的回复，于是模型看到的自己那 20 条过去全都不带标记，
   它照着自己的过去学，把 system 里那条格式要求悄悄丢了。实测对比（同一批提问）：

   | 条件 | 带标记 |
   | --- | --- |
   | 无历史（新客户端） | 3/3 |
   | 恢复真实 20 条历史 | **0/3** |
   | 历史 + 末尾补一条提醒 | 3/3 |

   所以修法不是改措辞，是**改位置**：在历史之后、用户这句之前，补一条
   `_EMOTION_REMINDER`（system）。要求写在 400 字的 system 开头，
   是会被后面几十条反例冲淡的 —— 这是"位置问题"，不是"模型不听话"。
   回归测试 `tests/test_emotion_wiring.py::EmotionPromptReminderTest`
   锁的就是这条提醒的**位置**，不是措辞。

**没做（属于"以后"）**：持久心情、情绪影响 TTS 语速音调。
（控制台脸渲染情绪 2026-09-18 已做，见上。）

### 4c 向量检索（完成即停手）

细节见 §6.1 的 P2。一句话：真语义检索跑通了，但**这个量级上它买不到体感**，
真正的瓶颈在 4b 的抽取质量，所以到此为止，不再加深。

---

### S5 · 表情表意修复（2026-09-18 深夜）

用户反馈：「眼睛移动逻辑有点问题（我往右它的眼睛往左）做成 TODO 后面再说；
先把表情样式确认下来，我觉得现在的表情还是差点意思。」

去查「差点意思」，结论是**大部分是 bug，不是审美**：

1. **豆眼整体画高了半只眼**。`bean(x, y, w, h)` 的第 2 个参数是眼睛**中心** y，
   调用处却按「上边缘」传了 `cy - eyeH / 2` —— 两只眼睛整体高了 42px（正好半只眼高），
   脸下半部空一大块。这是「看着不对」的主因。
   一个坐标写错，四个表情跟着一起垮：
   happy 的「弯月眼」雕花椭圆根本没碰到眼睛（所以笑眼一直只是普通豆眼）、
   angry/sad 的眉毛压在眼睛上、surprised 的挑眉贴着豆眼顶、像贴了根胶带。
2. **sad 的视线下垂写成了 `lookTargetY += 5`**（本意是固定偏移），每帧累加，
   实测 3 秒后 `lookY = 858` —— 眼睛直接飞出脸外。改成 `dropY` 常量偏移。
3. 顺手补的（低风险、一眼见效）：眼睛加一层柔和外发光（LCD 背光感，
   两只眼睛合成一条路径一次填完，`shadowBlur` 只算一遍 —— 这个操作很贵）；
   **情绪配嘴**（happy 上弯 / angry·sad 下弯 / surprised 小圆口）——
   之前怒/悲/惊只有眉毛在撑，嘴巴空着，情绪只表达了一半。
   另修一个隐藏 bug：说话时的嘴没显式设 `fillStyle`，会继承腮红/挖月牙留下的颜色，
   开心时边笑边说嘴是隐形的。

验证方式：`.scratch/shoot_face.py`（Playwright 驱动 msedge）+ `.scratch/sheet.py`
拼对照图，8 个状态一组、改前/改后各拍一遍。两个脚本都在 `.scratch/`（已 gitignore），
不算生产依赖，`requirements.txt` 不写 playwright。
工具自己也踩了个坑：`evaluate` 的参数名和页面全局变量重名，`mood = mood` 成了自赋值空操作，
第一轮情绪截图全是 idle、白拍一轮 —— 参数一律起 `st/md/kk/mo` 这种不撞车的名字。

**还等用户拍板**（不是缺漏）：neutral 要不要常驻一条淡嘴；
要不要给豆眼加瞳孔+高光（更像角色，但不再是纯 Vector 豆眼）；
面板造型（耳朵/天线/下巴）留给 Live2D 换皮那一步。

---

### B 阶段 · 平板第一步（2026-09-19 完成，待部署）

平板以前开不了这张脸（`face/server.py` 只起 WebSocket，伺不了网页），当时靠临时
`python -m http.server 8080` 顶着 —— 重启就没了，还得手输 IP。这一步把它做成正路：

- **网页和 WebSocket 共用一个端口**：`http://<主机IP>:8765/` 就是脸，页面里的
  `location.hostname` 天然是主机地址，**配对零操作**。真 token 由服务端注入页面
  （`<meta name="xiaoyu-token">`），平板不用带 `?token=`。静态文件解析在新增的
  `xiaoyu/face/static.py`（纯逻辑、好单测），**目录穿越就挡在这里** ——
  `/../.env` 返回 404，不然局域网里谁都能把 API Key 和记忆库打包带走。
- **断线看得见**：断线时脸整块变灰变淡 + 红点 + "断了，重连中……"，字幕同时清掉。
  以前最坑的是"脸还在屏幕上画着、其实早断了"，从画面上完全看不出来。
- **心跳 + 假死检测**：主控闲置时每 5 秒一个 `tick`（本来一句话都不发，页面分不清
  "安静"和"断线"）；页面 15 秒收不到任何消息就自己重连；重连间隔 3 秒 → 1 秒；
  页面重新可见 / 网络恢复时立刻踢一脚 —— 平板挂起会把 JS 定时器一起冻住，
  实测原来要 50 多秒才接得回来。
- **刚连上补推当前状态**：以前新脸要等"下一次状态变化"才知道现在什么状态，
  而重连时主控多半在待机，屏幕上就一直停在"连接中"。
- **平板尺寸适配**：画布从写死 520x420 改成按屏幕缩放并乘 `devicePixelRatio`
  （几何仍按 520x420 画，靠 `ctx.setTransform` 缩放 —— 眼睛和字一起变大、高分屏不糊；
  注意 `shadowBlur` 不吃画布缩放，得自己乘回去）；字幕/状态改成浮层，
  补上 `theme-color` 与 `apple-mobile-web-app-capable`，加到主屏幕后按全屏 App 起。

**验证**：真 Chrome（`channel="chrome"`）按 1280x800 与 800x1280 两种平板尺寸截图，
核对待机 / 说话+开心 / 听话+眼睛跟随 / 断线 / 重连五个状态，页面零报错；
测试 268 → 290，unittest 与 pytest 数字一致。冒烟脚本 `.scratch/_verify_face.py`。

**还没做**：跑着的进程还是旧代码 —— 自适应、心跳、补推状态要**重启一次**才生效，
和 S6a 眼睛镜像修复一起安排在浸泡收工（2026-09-23）当天。真人验收也留到那时：
平板打开 `http://<主机IP>:8765/` → 加到主屏幕 → 息屏改"永不" → 系统音量关 0 →
支架立起来 → 挂一整天不断线。

### 身体接口层 · 舵机这半边（2026-09-19 完成，未接真硬件）

用户 2026-09-19 给的目标形态是照片里那种（猫耳壳 + 约 2 寸 LCD 脸 + 舵机手臂），
并明确"先把软件适配好、再上硬件"。所以先把**不花钱、不等硬件**的那半件事做掉：
把 gaze 到舵机这条通路钉死，真舵机买回来只换 backend。

- 新增 `xiaoyu/body/`：`servo.py` 里的 `ServoBackend`（只要求 `set_angle` + `close`
  两个方法）和 `FakeServo`（只记日志 + 留轨迹，能单测）；`head.py` 里的
  `gaze_to_angles()` 和 `HeadLook`。行为代码只认接口 —— 这是 §8 里 `RON`
  那个项目最值得学的一点。
- **角度语义 = gaze 的语义**（`gx > 0` 人在这张脸的右边 -> `pan > 0` 头也往那侧转）。
  装反了走 `XIAOYU_BODY_INVERT_PAN` / `XIAOYU_BODY_INVERT_TILT`，**别去改
  `head.py` 的符号** —— 相机没镜像那次的返工就是"两边各反一次、最后谁也说不清
  反了几次"。
- 带**死区**（`XIAOYU_BODY_DEADZONE`，默认 0.05）：人脸检测本身在抖，没有死区
  头会一直微微晃。幅度默认左右 ±30° / 上下 ±15°，**先小后大**（真舵机瞬间大角度
  转动像抽搐，还容易堵转发热）。
- 接法：`app.attach_vision` 里 `tracker.on_gaze = _fanout("gaze", face.publish_gaze,
  head.look)` —— 眼睛和头吃同一份 gaze，否则"眼睛动了头没动"看着像两套东西。
  用 `_fanout` 是为了**身体炸了脸照收**（"情绪回调失败"那次的同类错误）。
- **默认关**（`XIAOYU_BODY_ENABLED=0`）：开着也只是在 DEBUG 日志里打角度，不动任何
  硬件。真舵机接上那天，把 `app.attach_body` 里的 `FakeServo` 换成真 backend 就行。

**还没抽的**：屏幕 / 麦克风 / 喇叭的抽象。现在抽等于凭空猜接口 —— 等真硬件定下来
（甲/乙/丙，见 [`docs/离开电脑_计划.md`](docs/离开电脑_计划.md) §4.3）再抽。

**验证**：`py -3.11 -m unittest discover tests` 290 -> **309**，pytest 数字一致；
另跑了真配置冒烟（`XIAOYU_BODY_ENABLED=1` 喂五帧 gaze，角度正确），
脚本 `.scratch/_smoke_body.py`。

### A 档 · 小智设备协议适配层（2026-09-19 开工，全程离线）

板子还没到，但协议是**有文档的** —— 所以按
[`docs/xiaozhi拆解.md`](docs/xiaozhi拆解.md) §6：**先从 §2.2.1「第一批交付物」开工**，
那一批 5 个文件不需要硬件、不需要联网，假客户端就能单测。

**第 1/5 步 · 真样本 fixture（本次完成）**

- 新增 `tests/fixtures/xiaozhi/`：**22 个 JSON 样本 + 1 个 README 溯源表**
  （`device/` 7 个 = 设备发给我们、`server/` 14 个 = 我们要回给设备）。
- **全部逐字抄自**上游 [`78/xiaozhi-esp32`](https://github.com/78/xiaozhi-esp32)（MIT）的
  `docs/websocket_zh.md`。抓取用 `Invoke-WebRequest`（本机 `curl.exe` 到 GitHub 被挡），
  快照 sha256 `d5791528bc41b507dd9b3703da1992190c8436559f2b0afdcc7e78acabdc5c32`、
  18021 字节；快照留在 `.scratch/xiaozhi/`（已 gitignore，没进仓库）。
- **「不是自己编的」有硬证据**：生成脚本对每个文件先断言
  `assert body in 文档原文`，对不上就生成失败；再断言是合法 JSON、
  `type` 非空（文档 §8.6：缺 `type` 设备端只记日志不执行）。README 里逐文件
  列了出处（§几）+ 字节数 + sha256 前 12 位，将来能复核。
- **两处刻意的多样性，别当重复删掉**：① 紧凑 / 缩进两种写法（`*_compact.json`）——
  JSON 不在乎空白，解析器两种都得认；② 有 / 无 `session_id`（`*_no_session.json`，出自
  文档 §6 的状态流转示例）—— 不能因为缺这个字段就炸。
- **没做成 fixture 的两处（别以为是漏了）**：文档 §8.6 的 `{"type": ...}` 是**占位符、
  不是合法 JSON**；§3.2/§3.3 的二进制协议 v2/v3 是 C 结构体描述、不是 JSON 样本
  （第一版只走 §3.1：裸 Opus 数据）。坏 JSON / 缺字段的用例由协议测试自己构造并引 §8.6。
- **一条前提要记住**：文档开头自己写着「该文档仅基于所提供的代码推断，实际部署时可能需要
  结合服务器端实现进行进一步确认」。所以板子到了以后，第一件事是**抓一次真机 hello 原文**
  跟 `device/device_hello*.json` 对一下；不一致**以真机为准**，回来更新 fixture。

**下一步（一次只做一个文件，做完停下来给用户看）**：`xiaoyu/xiaozhi/protocol.py` ——
纯函数：解析 device `hello`（`version`/`transport`/`audio_params`/`features`）、构造 server
`hello`（带 `session_id`、`audio_params` 24000Hz）、文本帧 JSON 编解码、binary(Opus) 与
text(JSON) 的判定；坏 JSON / 缺字段 / 版本不符要返回明确错误，**不许抛到调用方炸掉**。

**验证**：`py -3.11 -m unittest discover tests` 与 `py -3.11 -m pytest tests -q`
都是 **309**（fixture 是数据不是测试，数字不动，两个数字一致）。

### 下一步方向（用户 2026-09-18 定，按此顺序）

1. ~~**S6a 眼睛跟随**~~ —— ✅ 代码完成、冒烟通过，**只差真人验收**。
2. ~~**S5.5 常驻自愈**~~ —— ✅ 代码完成（2026-09-18），**只差跑一周**，见 §6.4。
3. **池子**（随时插队）：~~情绪系统~~ ✅ 第一层完成 / ~~4c 向量检索~~ ✅ 完成即停手 /
   **Live2D 换皮**（未开始）/ **完整 S6**（未开始）。
   > 插一句：**表情样式还在等用户拍板**（见上面的「表情表意修复」）。
   > 换皮要拿锁定的脸当设计基准，样式没定之前别开这一项。
4. **平板端**（用户明确排在 N100 之前）：把"脸"做成小新Pad 的专属客户端。
   2026-09-18 查证后有两个前置要先知道：**服务端现在只起 WebSocket、不伺服
   `face/index.html`**，平板根本打不开这张脸；而且一旦改成 HTTP + 局域网 IP，
   **`navigator.wakeLock` 会失效**（它要求安全上下文，`http://192.168.x.x` 不算），
   平板会自己熄屏。细节和做法见 [`docs/离开电脑_计划.md`](docs/离开电脑_计划.md) 第 3 节。
5. **N100 迁移**（最后）：下单（§6.2 三问）→ M2 便宜验证 → 迁移。

> 用户原话（2026-09-18）："N100先不急，先把1.2.4做完后，做平板端，做完再迁n100"。

---

## 7. 关键实测数据（别再重新测一遍）

| 环节 | 实测耗时 | 说明 |
|------|----------|------|
| VAD 等你说完 | 0.5 秒 | 可调（`XIAOYU_SILENCE_SECONDS`），原为 0.8 |
| SenseVoice 识别 | 0.13~0.15 秒 | 本地。**反复调都一样，没有冷启动问题，不用预热** |
| DeepSeek 出第一句（冷连接） | **1.99~2.29 秒** | 其中约 1.5 秒是 TCP + TLS 握手 |
| DeepSeek 出第一句（预热后） | **0.53 / 0.61 / 0.65 秒** | 历史 0~40 轮都一样，跟上下文长度无关 |
| **本地 TTS 合成一句** | **0.037 ~ 0.35 秒** | RTF 0.053~0.080。33 字的长句也只要 0.33 秒 |
| local TTS 加载模型 | 1.4~1.9 秒 | 启动时一次性，`warmup()` 之后不算在回复里 |
| edge-tts 出整句（2026-09-18 复测） | 1.6~3.5 秒 | **服务端生成时间，预热/stream 均无效**（§6.3），已退回可选项 |

**用户实际等多久（从你说完话到它开口）**：

    VAD 0.5 + 识别 0.14 + 大模型首句 0.6 + 合成 0.1 ≈ 1.35 秒

改造前这条链是 `0.5 + 0.3 + 2.3 + 4（edge-tts 中位数）≈ 7 秒`。

**两个候选音色的实测对比**（Ryzen 5 5600H，2026-09-17）：

| 模型 | 大小 | 加载 | 合成 33 字 | RTF | 备注 |
|------|------|------|-----------|-----|------|
| `vits-piper-zh_CN-huayan-medium` | 60MB | 1.4s | 0.32s | 0.057 | 自带声码器；**默认** |
| `matcha-icefall-zh-baker` | 72MB | 1.9s | 0.36s | 0.059 | 要配 `vocos-22khz-univ.onnx`（51MB）；有 OOV 警告 |

声卡现状（2026-09-17 实测，MME 这一组）：

- `[1] 麦克风 (Realtek(R) Audio)` —— 输入
- `[3] 扬声器 (Realtek(R) Audio)` —— 输出（注意：**曾经是 `[4]`**）
- `[3或4] H25T7-3 (NVIDIA High Definition)` —— 显示器，系统默认输出，**别用**

`.env` 现在按名字选：`XIAOYU_SPK_DEVICE_NAME=扬声器` / `XIAOYU_MIC_DEVICE_NAME=麦克风`。

---

## 8. 参考过的开源项目（真实 star 数，2026-09-17 查）

用户自己找了两个，评估如下：

- `RiccardoDominici/MemoryWebAssistant` —— **3 stars**，单个大文件，架构参考价值有限。
  只有两个小点子值得偷：RAG/CAG 双模式、记忆查重（相似度阈值 0.8）。
  别跟它的 Ollama 本地小模型（中文质量差）和 faster-whisper `large-v3`（4GB 显存吃力）。
- `well-it-wasnt-me/RON`（项目名 DeskBot）—— **1 star，但架构值得学**。
  硬件抽象在 protocol 接口后面（`Display`/`ServoController`/`AudioOutput`/`Microphone`/
  `Camera`/`LLM`/`EventBus`），行为代码永不 import 具体驱动，同一套代码能跑真机 / mock / 无头仿真。
  **这就是 S5/S6 和"搬到 N100"的正解。** 别照搬它的树莓派 + 舵机 + Q-learning 强化学习，太重。

更该看的：

| 项目 | Stars | 语言 | 备注 |
|------|-------|------|------|
| `78/xiaozhi-esp32` | 30.0k | C++ | 中文语音助手的事实标准，极活跃 |
| `xinnan-tech/xiaozhi-esp32-server` | 10.6k | Python/Java/Vue | 后端就是完整的 VAD→ASR→LLM→TTS + 打断，**架构图最值得抄**；记忆有 `mem_local_short` / mem0 / PowerMem，知识库接 RAGFlow（比我们的 4a/4b/4c 更「配置化」，但依赖外部服务，我们是本地自研） |
| `m5stack/StackChan` | 1.3k | C++/Dart/Go | M5Stack 官方整机（2026-01 建库）：ESP-IDF 固件 + Flutter App + Go 服务端，`firmware/` 和 `server/` 各自 MIT，**仓库根目录没有 LICENSE**。DIY 原版是 `meganetaaan/stack-chan`（1.7k / Apache-2.0 / TypeScript） |
| `Open-LLM-VTuber/Open-LLM-VTuber` | 13.8k | Python | Live2D + 语音打断，和 S5 对口；但 2026-05 后没更新 |
| `moeru-ai/airi` | 49.2k | TypeScript | 终态参考，很重，现在看不懂正常 |
| `k2-fsa/sherpa-onnx` | 14.8k | C++ | 已在使用，KWS + ASR + TTS 一家全包，**别再加别的音频库** |

反面例子：`rhasspy/piper` 有 11.2k stars 但**已停止维护（archived）**，
活跃继任者是 `OHF-Voice/piper1-gpl`。`dscripka/openWakeWord` 2.8k stars
且 2025-12 后没更新 —— 所以当初选 sherpa-onnx KWS 是对的，不用换。

---

## 9. 给下一个 AI 的提醒

- 用户说"读文档"就是让你读这个文件。
- **不要一次塞太多东西**。他每周只有 3 小时，一次做一件事做完再下一件。
  这个项目最大的风险不是技术难度，是收藏了一堆项目结果一个都没跑通。
- 改完代码要更新本文件的**第 0 节和第 6 节**，然后提交 + push。
- **路线是用户定的，别擅自改顺序**：S4 记忆 → 桌面独立成体（N100）→（以后）随身形态。
  见第 0 节和 §6.2。他想做"随身携带的独立个体"，但明确说了目前只做桌面这一版。
- 说话直接一点，有问题就说有问题，别为了让他高兴而说"已完成"。
- **冒烟脚本要接"线上同一段代码"**。`smoke_emotion.py` 当初自己写了个签名正确的
  `lambda m, i`，于是冒烟一路绿灯、线上必炸（用户实测才发现）。凡是"接线"类的东西
  （回调、路由、参数顺序），冒烟必须走和主循环同一个函数，不能自己另接一遍 ——
  另接一遍等于测了个假货。