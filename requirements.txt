# ============================================================
# 分阶段安装，不要一次全装（详见 docs/ 里的总方案 3.4 节）
#   pip install -r requirements.txt
# ============================================================

# ---------- 核心：日志 + 音频基础（S0 就要）----------
loguru>=0.7.2
sounddevice>=0.5.1
numpy>=1.26
soundfile>=0.12

# ---------- 唤醒 + 语音识别（S1 / S3）----------
sherpa-onnx>=1.10

# ---------- 语音合成（S1，播放走 sounddevice，不需要 pygame）----------
edge-tts>=7.0

# ---------- 唤醒词工具（生成音素格式的唤醒词）----------
pypinyin>=0.53

# ---------- 大模型（S2）----------
openai>=1.40

# ---------- 表情脸（S5，主控和脸之间的 WebSocket）----------
websockets>=12

# ---------- 表情资产（离线出图，S5 设备端）----------
# 主程序**不需要**它：PC 那张脸是浏览器画的，设备端用的是预渲染好的图。
# 只有 scripts/build_face_assets.py 和它那批像素测试要。
# 没装的话主程序照跑，那批测试会自己跳过。
Pillow>=10

# ---------- 记忆（4c 向量检索；可选：没装就退回"最近几条事实"）----------
fastembed>=0.2
# sentence-transformers>=3.0   # 旧重路线，连带 2GB PyTorch，不用装

# ---------- 眼睛跟随（S6a，可选：装不上就静默禁用）----------
# mediapipe>=0.10
# opencv-python>=4.10

# ---------- 视觉完整版（S6 可选）----------
# rapidocr-onnxruntime>=1.4
# insightface>=0.7.3
# onnxruntime>=1.18

# ---------- 开发 ----------
# pytest>=8.0
# pyserial>=3.5         # 板子到货后用来看串口日志（2026-09-20 已装 3.5；代码不依赖它）
