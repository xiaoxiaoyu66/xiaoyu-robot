# ============================================================
# 分阶段安装，不要一次全装（详见 docs/ 里的总方案 3.4 节）
#   pip install -r requirements.txt
# ============================================================

# ---------- 核心：日志 + 音频基础（S0 就要）----------
loguru>=0.7.2
sounddevice>=0.5.1
numpy>=1.26
soundfile>=0.12

# ---------- 唤醒 + 语音识别（S1 / S3）----------
sherpa-onnx>=1.10

# ---------- 语音合成（S1）----------
edge-tts>=7.0
pygame>=2.6

# ---------- 大模型（S2）----------
openai>=1.40

# ---------- 记忆（S4 再装，会连带下载约 2GB PyTorch）----------
# sentence-transformers>=3.0

# ---------- 视觉（S6 可选）----------
# rapidocr-onnxruntime>=1.4
# opencv-python>=4.10
# insightface>=0.7.3
# onnxruntime>=1.18

# ---------- 开发 ----------
# pytest>=8.0