"""视觉层（S6）。

S6a 眼睛跟随（当前）：摄像头 + BlazeFace 读人头位置，眼睛跟着人看。
    tracker.GazeTracker  —— 守护线程，只在待机/听话时跑
    gaze.face_to_gaze    —— 鼻尖偏移 -> 归一化 gaze 的纯函数（可单测）

完整版 S6（以后）：读字 RapidOCR、认人 InsightFace、扫码 OpenCV，
走意图路由（命中"看看 / 这是谁 / 写的什么"才开摄像头）。

原则：人脸框只在本机算，不出机器、不存图（v3 数据边界精神）；
视觉是可选件，任何一步失败都静默降级，不影响语音主流程。
"""
