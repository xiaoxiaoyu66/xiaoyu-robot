# TODO

> 想做但先记着的事。做完了就把 `- [ ]` 改成 `- [x]`，不用删。

> **只想知道“现在做到哪了、下一步干什么”，去看 [`HANDOFF.md`](HANDOFF.md)。**
> 这份文件是长期的待办池，不是当前的作战计划。

## 路线（用户 2026-09-18 定，别擅自改顺序）

最终想做**随身携带的独立个体**：不依赖电脑，揣在兜里就能用。
但**目前只做"桌面独立成体"**，随身以后再说。

    S4 记忆  →  桌面独立成体（N100，插电放桌上）  →  （以后）随身形态

计划细节见 [`HANDOFF.md`](HANDOFF.md) 的 §6.1 和 §6.2。

## 已完成

- [x] **反应慢的第一轮优化** —— 唤醒命中后立刻播短提示音、
      `silence_seconds` 0.8 -> 0.5、persona 加上每句 25 字上限。
- [x] **反应慢的根治 · TTS 换成本地** —— edge-tts 每句话都要连一次微软服务器
      （实测 0.88~11.76 秒，另有 2 次直接失败），换成 sherpa-onnx 本地合成，
      每句 0.04~0.35 秒，RTF 0.06。edge-tts 保留成可切换的"高音质模式"。
- [x] **反应慢的根治 · 大模型连接预热** —— 实测第一次请求 2.0~2.3 秒、
      之后只要 0.5~0.8 秒，多出来的全是 TCP + TLS 握手。
      `DeepSeekClient.warmup_async()` 启动时和唤醒后各预热一次（和录音+识别并行）。
      首句 2.05 -> 0.41 秒。
      量延迟用 `python scripts\bench_latency.py`。
- [x] **语音模式真人验证** —— 喊"小宇"走完一轮，用户原话
      "唤醒词可以了，延迟也没那么明显了"。S1/S2/S3 到此全部通过真人验证。
- [x] **唤醒词换成「小柚子」+ 顺带让脚本支持英文** —— 2026-09-18。
      `config/keywords.txt` 现在是 `x iǎo y òu z i :2.5 #0.5 @小柚子`；
      `config/persona.md` 里它自称也统一成了"小柚子"。
      `make_keywords.py` 现在中英文都能生成 —— 英文查模型自带的 `models/kws/en.phone`
      （CMUdict 格式，12.6 万条），**不用装 g2p_en / nltk**。
      ⚠️ 顺带查出来一件事：`小宇` / `小雨` / `晓宇` 的音素**完全一样**（都是 `x iǎo y ǔ`），
      模型根本分不出来 —— 这就是以前误唤醒偏多的根因。三音节的词能治一大半。
- [x] **定音色** —— 2026-09-18。从 edge 云端中文音色里挑中
      `zh-CN-XiaoxiaoNeural` + 语速 `+6%` + 音调 `+12Hz`（升调那一版）。
      试听工具：`python scripts\preview_voices.py --edge`。
      代价要知道：edge 每句都要跟微软握手，首句 **1~3 秒**（本地模型是 0.1~0.3 秒）。
      想要"好听又本地"，试了 Kokoro 多说话人模型（`python scripts\download_models.py --only kokoro`）。
      **本机实测跑不动**：RTF 1.3~1.6，合成比实时还慢（piper 才 0.06~0.08），
      加到 6 线程也一条线 —— 不是并行度的问题。引擎代码留着了（下载项也在），
      将来换到有 GPU / 更强的机器再试。完整数据和判据见 `HANDOFF.md` §6.3。
      相关：`TtsConfig` 补上了 `pitch`，并修掉一个隐藏 bug ——
      `.env` 里的 `XIAOYU_TTS_RATE` / `XIAOYU_TTS_VOLUME` 以前**根本没被读取**。
- [x] **补上记忆的回归测试** —— 2026-09-18。以前 `MemoryStore` 在 `tests/` 里
      **一个引用都没有**（grep 零匹配），而 S4 的三个台阶全都要动它。
      新增 `tests/test_memory.py`（重启后读得回来、备份、facts/recall）
      和 `tests/test_llm_history.py`（历史读回、时间感、兜底话术）。
      全套测试 86 → 144 个。

---

## 环境

- [ ] **迁移到虚拟环境（规范做法，当前图省事用的系统 Python）**

      现在依赖装在系统 Python 里，能用，但和别的项目混在一起，
      将来容易出"这个包被别的项目升级了导致这边跑不了"的问题。
      等以后搬到 N100 上时顺手做掉：

      ```powershell
      cd "D:\JavaAI\XiaoYu Robot"
      python -m venv .venv
      .\.venv\Scripts\Activate.ps1
      pip install -r requirements.txt
      ```

      做完之后每次开工第一件事就是激活它，提示符前面会出现 `(.venv)`。

---

## 功能

- [x] **S4 · 重启之后还记得你（4a）** —— 2026-09-18 完成。
      `DeepSeekClient` 启动时用 `memory.recent_messages(max_history, include_time=True)`
      把最近的对话读回 `_history`，并在 system 里注入时间感（当前时间 + 上次聊天时间）。
      实测（真实库）：26 条对话 → 读回 20 条，提示词里出现
      "现在是 2026-09-18 周五 14:33。你们上一次聊天是昨天 22:04。"
      顺带做掉三件相关的：读回时丢掉没等到回复的收尾提问、API 失败改说一句人话
      （`_speakable_error`）、启动时给 `data/xiaoyu.db` 留一份当日备份。
      **真人验证通过**（2026-09-18 晚）。
- [x] **S4 · 自动总结"关于主人的事实"（4b）** —— 2026-09-18 完成。
      以前 `facts` 表建好了、`add_fact()` 也写好了，但 **一个调用方都没有**，
      所以 facts 永远是 0 条、`recall()` 永远返回空。
      现在每聊够 `XIAOYU_MEMORY_SUMMARIZE_EVERY` 轮（默认 20）就在**后台**调一次模型，
      从最近的对话里抽出"关于主人的事实"存进库。
      判重的做法：把已有事实**连 id 一起**给模型，让它直接说"这条跟 id=3 重复"，
      比拿两段文字做字符串比对可靠得多。输出要求是 JSON，解析做了容错
      （会剥 ``` 围栏、能忍前面的客套话，解析不出来就当这轮没整理出东西）。
      整理全程允许失败，只记日志 —— 记忆是加分项，不能因为它把对话带崩。
      调试时把 `XIAOYU_MEMORY_SUMMARIZE_EVERY` 调小（比如 3）。
      新增 `tests/test_fact_summary.py`，全套测试 144 → 172 个。
      **真人验证通过**（2026-09-18 晚）：调小阈值聊几轮后，问"我叫什么"答得上来。
- [x] **S4 · 记忆的向量检索（4c）** —— 2026-09-18 完成，**到此为止不再往下做**。
      `recall()` 从"最近记得的事实"换成真语义检索：fastembed 加载
      `BAAI/bge-small-zh-v1.5`（512 维、约 90MB、ONNX、**无 torch**），
      事实向量缓存在 `fact_vectors` 表（按 fact_id 一对一，文本没变就不重算），
      余弦相似度取 top_k，低于 0.25 当作不相关；拿不到结果就退回"最近几条"。
      fastembed 没装 / 模型下不动 → 整条记忆主流程一行不受影响（可选件静默降级）。
      真编码实测：问「他叫什么名字」→ 主人叫小林；「猫是什么颜色的」→ 橘猫；
      「他在忙什么」→ 正在准备考研，都对。新配置 `XIAOYU_MEMORY_MODEL`。
      **为什么停手**：facts 表现在就是这个量级（几十条），向量检索和"全量注入"
      没有可感知差别；真正的瓶颈是 4b 的抽取质量和注入策略，不是检索算法。
      再加时间衰减 / 混合检索 / 评测集属于白投入。

- [x] **S5 · 表情脸第一版（协议 + 触屏打断 + 最简脸）** —— 2026-09-18。
      WebSocket 协议（state/mouth/caption 三类事件 + interrupt 命令，
      token 校验，错 token 关闭码 4401）；触屏打断（点脸 → 停播 → 0.2s → 开麦，
      半双工不破；键盘模式也接了，调试不用麦克风）；
      最简脸 `face/index.html`（Canvas 圆脸 + 眨眼 + 嘴跟音量 + 状态光环 + 字幕，
      单文件零构建）。**真人验证通过**（2026-09-18 晚）：`--text` 模式开脸聊天，
      状态/字幕/口型跟随、点脸立停，终端字符画脸同步可见。
- [ ] **S5 · Live2D 换皮** —— Vue3 + pixi-live2d-display（pin pixi.js@6，
      库已停更但可用），协议不变只换消费端。旧手机/平板浏览器当屏幕
      （小新Pad 可以，现在不用买）。模型文件注意授权：自用别打包上传 GitHub。
- [x] **情绪系统** —— 2026-09-18 完成第一层，**就做到这里**。
      做法：让 DeepSeek 在每轮回复**末尾**输出 `[emotion]happy,0.8[/emotion]`，
      跟着流式正文一起回来 —— 零额外调用、零额外延迟，正文和情绪同一次生成，
      语义必然一致。解析不出来一律 neutral（情绪是表演，不能带崩对话）。
      协议加 `{"type":"emotion","mood":..,"intensity":..}` 增量事件；
      脸按 mood 改眼形 + 光环色（生气锐眼红光 / 难过垂眼蓝灰 / 开心弯眼暖金 /
      惊讶瞪圆青），12 秒没新事件回 neutral。上一轮情绪回灌下一轮 system 做语气联动，
      但明确要求**只表演、不伤人**。
      **踩过的坑（已修 + 有回归测试）**：流式 token 边界会切在 `[emotion]` 中间，
      `split_visible()` 一开始只认完整开标记，于是 `[` 被当正文发出去、
      整个标记跟着漏进正文/字幕/历史。逐字喂必现，真 DeepSeek 冒烟也复现过。
      → 修法是 `_holdback_index()` 连"开标记的前缀残片"一起按住。
      没做（属于"以后"）：持久心情、控制台脸渲染情绪、情绪影响 TTS 语速音调。
- [x] **S5.5 · 常驻自愈** —— 2026-09-18 完成，**待真人验收**（拔插 USB / 断网一分钟）。
      四件事：
      1. **主循环自愈**：`run_voice_loop()` 拆出 `_one_conversation()`，
         单轮异常只记日志 → 等 2 秒 → 回待机；连续失败 5 次才退出，
         退出原因写 `logs\error.log`，交给进程守护重启。
      2. **VAD 决断 + 自适应噪声底**：开机先采 0.3 秒环境噪声，
         静音阈值 = 噪声底 × 4.0（夹在 0.008~0.08）；录完用 silero-vad 判有没有人说话，
         模型缺失就退回振幅启发式。**搬 N100 换麦克风后固定阈值会失效，这步是为搬家做的。**
      3. **开机自启 + 进程守护**：`scripts\install_autostart.py`（注册计划任务 + 电源设置：
         接电永不睡眠）＋ `scripts\run_forever.cmd`（挂了 5 秒自己爬起来）。
      4. **误唤醒率有数**：唤醒时打一行 `WAKE_EVENT | ts=... | keyword=...`，
         `python -m xiaoyu --wake-report` 按天统计。
      顺带查到的事：旧唤醒词有 **6 次唤醒挤在 0.8 秒内**的误唤醒记录 ——
      换「小柚子」就是冲它去的。
- [ ] **S6a · 眼睛跟随（用户 2026-09-18 提出，插队优先）** —— 摄像头读人头位置，
      脸的眼睛看向人：人在左边它往左看，低头它往下看（Vector 的招牌"盯着你看"）。
      先用电脑摄像头，其他形态（外接/USB/深度相机）以后再说。
      **代码已完成（2026-09-18），冒烟通过，待真人验收**：
      vision/gaze.py 纯函数（鼻尖偏移→gaze∈[-1,1]² + EMA）+
      vision/tracker.py 守护线程（只在 IDLE/LISTENING 取帧，10Hz 节流，
      检测不到就停发、豆眼 500ms 超时回游荡）+
      协议加 `{"type":"gaze","x":..,"y":..}` 增量事件 + 豆眼待机/听话时优先跟随。
      mediapipe 1.0.1 用 Tasks API（旧 mp.solutions 已删），模型 blaze_face_short_range
      .tflite 约 225KB 放 models/vision/。验收：启动后脸应盯着你看，转头/低头跟着动。
      验收通过再勾。原设计骨架全部落地：检测 BlazeFace 短距（本机算不存图）、
      gaze 纯函数 EMA 可单测、只在 IDLE/LISTENING 跑、协议纯增量、超时回游荡。
- [ ] **S6 · 眼睛（完整版）** —— RapidOCR 读字、InsightFace 认人，走意图路由（命中"看看/这是谁"才开摄像头）。
      S6a 是它的先行子集，两者共用摄像头接管逻辑。
- [ ] **桌面独立成体（搬到 N100）** —— 当前目标，详见 HANDOFF §6.2。
      已经量过：N100 跑得动整条链路，一轮对话约 1.8~2.2 秒（推算，待实测）。
      搬之前要先定两件事：**买哪台**（注意大部分 N100 是 DC 圆口不是 USB-C）、
      **装 Linux 还是 Windows**（各有代价，HANDOFF 里列了）。
      代码本身已经"搬家友好"：没有平台专用依赖，路径都走 pathlib，
      唯一要改的是 `.env` 里的音频设备名。
- [ ] **随身携带形态** —— 用户的最终方向，但**明确说了以后再做**。
      必须先有桌面版的功耗/发热/电池实测数据，否则是瞎猜。

## 体验

- [x] **真机验证接续对话** —— 2026-09-18 真人验证通过：唤醒后连聊 3 轮、不说话回待机。
- [ ] **打断功能（语音）** —— 说话时喊唤醒词能打断。**前提是先解决回声问题**：
      外放喇叭 + 开麦，它会听见自己。要么用耳机，要么上回声消除。
      （触屏打断已实现，这里剩下的是"语音打断"。）
- [x] **定音色** —— 2026-09-18 定稿，见上面"已完成"里那条。
      旧的本地试听文件（`data/voice_1_huayan.wav` / `data/voice_2_baker.wav`）可以留着不管。
      **当晚更新**：实测 edge 首句 1~3 秒全是微软服务端生成时间（预热无效、
      stream 边下边播首块 ≈ 整句下完，数据在 HANDOFF §6.3），**默认引擎已改回本地 piper**；
      edge 晓晓参数留在 `.env`，想听一行切回。
- [ ] **试语速** —— `.env` 里 `XIAOYU_TTS_RATE` 现在 `+6%`。
      再快会显得敷衍，再慢会显得没精神，这个只能自己听。

## 已知限制（不是 bug，是当前的设计取舍）

- 严格**半双工**：说话时不开麦。用外放喇叭时这是必须的，否则它会自己唤醒自己。
- 唤醒词只支持**中文或英文**，且必须是词表里有的音素（用 `scripts\make_keywords.py` 生成）。
- `--text` 键盘模式**会写记忆**（和语音模式共用 `data/xiaoyu.db`）。
  以前 TODO 里写的是"不会写"，那是错的 —— 实测 `run_text_mode` 把 memory 传给了
  `DeepSeekClient`，而 `stream_reply` 的 finally 无条件调 `remember_exchange()`。
  行为是有意的（一致、好排查），但要知道：**在键盘模式里调试说的话，
  会进入语音模式的上下文**。想干净地试，就先备份一份 `data/xiaoyu.db`。
- 记忆库每天第一次打开会自动备份成 `data/xiaoyu.db.YYYYMMDD.bak`（当天不覆盖）。
